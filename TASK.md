更新 TODO：下一阶段 World Skin 任务整理

目标

请更新 TODO.md，把下一阶段工作整理为清晰的开发任务列表。

本轮只更新 TODO，不要直接实现功能。
不要修改核心代码。
不要重构视觉系统。
不要新增实际功能。
只整理下一阶段任务，并保持与 MAP_SPEC.md 一致。

⸻

当前下一阶段需要纳入 TODO 的方向

TODO A：加入基于远近纵深的圆点视觉变化

目标：

在当前 pitch 地图视角下，加入基于屏幕纵深的视觉衰减，让远处和近处的点阵产生更自然的空间层次。

方向：

* 屏幕上方 / 远处：圆点略小、略淡、颜色略冷、浮起高度略低。
* 屏幕下方 / 近处：圆点略大、略实、颜色略暖或略清楚、高度线略明显。
* 不使用真实 blur。
* 不使用 heavy post-process。
* 不使用 shadowBlur。
* 不破坏 screen-stable density。

注意：

景深只是视角层修饰，不是新的感知轴。
不要让它改变记录数据。

⸻

TODO B：修改提示词 / 词语选择系统

目标：

重构采样完成后的感知词提示系统，使它更适合新的四条主观感知轴与声音轴结构。

当前目标轴：

* spaciousness：空旷 ↔ 密集
* gravity：轻盈 ↔ 沉重
* tension：松开 ↔ 绷紧
* flow：静止 ↔ 流动
* soundVector：环境声音轴，不由用户直接选择

需要整理：

* 词语选择每轮的提示文案。
* 每轮词语出现的逻辑。
* skip 逻辑。
* 2–3 轮选择的节奏。
* 词语是否覆盖不足时的补足规则。
* 不要让用户看到轴名称、参数或算法说明。

用户可见文案必须保持日文。

可保留方向：

* いまの感覚に近い言葉を選ぶ
* もう少しだけ、近いものを選ぶ
* 最後に、残った感覚を選ぶ
* スキップ
* 記録されました

需要后续再具体优化，不在本轮实现。

⸻

TODO C：扩充并重标感知词库

目标：

扩充当前感知词库，并按新的轴结构重新标注词语参数。

新的词库结构应服务于：

要求：

* 先从小型高质量词库扩展到中型词库。
* 不要一口气做过大词库。
* 每个词应可影响多个轴。
* 不要把词语简单归类为单一标签。
* 词语在 UI 中只作为可选感知词显示，不显示参数。
* 地图上不显示词语本身。

建议阶段：

1. v1：保留当前约 32 个词，完成新轴迁移。
2. v2：扩展到约 48–64 个词。
3. v3：根据实机测试再扩大词库。

⸻

TODO D：重做 Sense 呼吸系统

目标：

进一步重做记录界面的呼吸系统，使 10 秒采样更像 Apple Watch Mindfulness / 正念呼吸体验。

需要检查和优化：

* 呼吸圆环是否足够大。
* 呼吸幅度是否明显。
* 动画是否顺滑。
* 是否仍残留声音波形 / 音量条 / 频谱表现。
* 手机震动是否按节奏触发。
* 不支持 Vibration API 时是否静默 fallback。
* 长按采样与呼吸动画是否自然衔接。
* 完成后是否顺畅进入多轮词语选择。

要求：

* 记录界面不显示声音数据。
* 麦克风采样在后台进行。
* 视觉上只表现停留、呼吸、感知。
* UI 文案必须是日文。
* 不要加入倒计时数字作为主视觉。
* 不要做冥想 App 式疗愈插画。
* 仍然保持 World Skin 的冷静、暗色、身体性。

⸻

TODO E：改进 My Radius 与 World Map 的关联性

目标：

改进个人信息与世界信息之间的关系，让 My Radius 和 World Map 不只是两个割裂的视图。

当前原则：

* World Map：公共 / 模拟聚合数据。
* My Radius：本机个人记录 / localStorage。
* 两者不能混淆。
* 个人数据不能污染 World Map 聚合数据。

需要探索的方向：

1. 在 My Radius 中显示个人记录如何叠加在世界皮肤之上。
2. 在 World Map 中保留公共聚合视图，但可轻微提示“我的记录”在其中的位置。
3. 让个人记录与周围公共皮肤产生对照关系，例如：
    * 我的感知与周围聚合皮肤一致；
    * 我的感知与周围公共皮肤偏离；
    * 我常去的地方在 World Map 中是空洞 / 边界 / 高密度区域。
4. 不做社交系统。
5. 不做排行榜。
6. 不做用户贡献榜。
7. 不做点赞或公共上传机制。
8. 不做普通 marker。

可以考虑的概念：

但本轮只加入 TODO，不实现。

⸻

TODO 优先级建议

请在 TODO.md 中按优先级整理：

High Priority

1. 加入 depth-based 圆点远近视觉变化。
2. 重做 Sense 呼吸系统。
3. 重构词语提示与词库到新轴系统。

Medium Priority

5. 扩充词库到 48–64 词。
6. 改进 My Radius 与 World Map 的关联性。
7. 调整多轴颜色合成，保留低饱和色谱变化。
8. 优化高度线与圆点连接处的视觉问题。

Low Priority

9. PWA 主屏幕展示优化。
10. 进一步拆分 CSS / 数据文件。
11. 后续再考虑 JS 模块化。
12. 作品集录屏与说明文案整理。

⸻

TODO.md 更新格式建议

请将 TODO.md 整理为以下结构：

TODO

Current Focus

简要写当前阶段目标：

* 手机端稳定
* 视觉系统收束
* 感知词系统重构
* 作品集 demo 准备

High Priority

列出高优先级任务。

Medium Priority

列出中优先级任务。

Low Priority

列出低优先级任务。

Deferred

列出暂缓任务，例如：

* 大规模 JS 模块化
* WebGL / Three.js
* 真实服务器同步
* 社交功能
* 复杂 PWA 离线策略

Done / Recently Completed

保留最近已完成的任务摘要。

⸻

本轮限制

本轮只更新 TODO.md。

不要：

* 不要修改 test0.2.html
* 不要修改 index.html
* 不要修改视觉代码
* 不要实现 depth 变化
* 不要实现词库扩充
* 不要实现呼吸系统
* 不要实现 My Radius / World Map 关联
* 不要重构项目结构
* 不要改变 UI 文案
* 不要新增功能
* 不要恢复 jitter / 粒子 / marker / POI / 结构线

⸻

MAP_LOG 更新要求

如果本轮只更新 TODO，也请在 MAP_LOG.md 追加一条简短记录：

TODO Roadmap Update / YYYY-MM-DD

Problems

* 下一阶段任务方向分散，需要整理优先级
* 需要将 depth 视觉、词库扩充、呼吸系统、个人/世界关联纳入计划

Changes

* 更新 TODO.md
* 增加 depth-based 远近视觉变化任务
* 增加词语提示系统重构任务
* 增加词库扩充任务
* 增加 Sense 呼吸系统重做任务
* 增加 My Radius / World Map 关联性任务

Files Modified

* TODO.md
* MAP_LOG.md

Known Limitations

* 本轮只更新计划，不实现功能

Next Task

* 根据优先级逐条生成实现任务
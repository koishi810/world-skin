# MAP_SPEC

更新时间：2026-05-25
依据：当前实现 `test0.3/`（app.js · index.html · styles.css）、`MAP_LOG`、`UI_skill/`，以及历史项目说明书《世界皮肤 / World Skin》v0.2。

## 项目定位

项目名：世界皮肤 / World Skin

项目形式：手机端 Web 原型 / 感知型地图系统 / 城市体验可视化实验

一句话说明：

《世界皮肤》是一套以身体感知为基础的地图记录系统。它不使用评分、推荐、排行榜或打卡图像来理解城市，而是让用户在具体地点停留片刻，通过声音、移动状态、时间与个人感受，将日常空间转译为一张具有纹理、噪点、厚度与空洞的"皮肤"。

## 核心问题

本项目不是为了做一个更方便的地图，而是回答：

当地图不再以消费判断为中心，而以身体经验为中心时，它会变成什么？

传统地图关注路线、店铺、坐标、评分和热度。《世界皮肤》关注粗糙度、密度、波动、空白、边界和痕迹。

## 设计目标

### 从评价地图转向感知地图

- 不使用星级、点赞、排行榜、热门地点等指标。
- 不判断地点好坏，只记录地点的强度和感知状态。
- 地点不作为消费对象呈现，而作为身体经验的沉积面呈现。

### 从打卡行为转向身体停留

- 记录必须发生在现场。
- 用户需要长按约 10 秒，形成短暂的停留。
- 记录不是"我到过这里"，而是"我在这里停留并感受过"。

### 从地点集合转向强度场

- 单个地点不是孤立点，而是皮肤上的局部纹理。
- 多次记录后，生活圈逐渐形成由噪声、时间、移动状态和个人痕迹构成的强度场。

## 核心功能

### 体感记录

用户在实际地点长按 10 秒，完成一次身体性记录。

记录内容包括：

- 环境声音：平均音量、音量起伏、噪声峰值、持续嘈杂或安静状态。
- 位置与移动：停留、缓慢移动、快速穿过。
- 时间：早晨、白天、夜晚、平日、周末，后续可扩展天气。
- 简短主观标记：例如浮く、重い、乾く、ざらつく、眠い、遠い、詰まる、ほどける。

当前代码原型已实现：

- 麦克风音量采样；权限拒绝时 `audioReady` 保持 false，下次按下时自动重试（不立即报错）。
- 麦克风不可用时模拟噪波。
- 10 秒长按记录（DURATION = 10000ms）。
- 时间段描述：morning / day / evening / night。
- 32 词 v2 感知词库（`SENSE_WORD_LIST`），多轮选择后聚合为 `senseVector`（spaciousness / gravity / tension / flow）；旧 `textureModifier` 仅作 legacy fallback。
- 记录写入 `soundVector`：loudness / turbulence / sharpness / continuity / texture。
- 个人记录从 `MY_SKIN_DATA`（`data/my_data.js`）种入，用 `SEED_KEY = "world-skin-v03-seeded-r1"` 防重复。

### 我的半径

以用户日常活动范围为基础生成个人感知范围。

它不是社交生活圈，也不是通勤效率圈，而是被记录逐渐显影的身体经验范围。

需要呈现：

- 哪些路经常经过。
- 哪些地方已经形成纹理。
- 哪些地方仍然空白。
- 哪些区域只有白天记录，没有夜晚记录。
- 哪些空间明明很近，却从未被真正感知。

### 空洞与孤岛

空洞：

- 周围已有较多记录，但中间仍然空白的区域。
- 用于提示用户可能长期绕开了某个地方。

孤岛：

- 大片空白中出现的孤立记录点。
- 用于提示某次偶然经过形成的异常痕迹。

### 边界带

当相邻区域呈现明显不同的皮肤状态时，显示感知边界。

边界带不是路线推荐，而是探索提示：沿着差异移动，观察城市在哪里发生了变化。

### 时间剖面

同一地点在不同时间段具有不同皮肤。

需要支持对比：

- 早晨 / 午后 / 夜晚。
- 平日 / 周末。
- 晴天 / 雨天（后续）。
- 同一地点的沉默、活跃、空洞和波动变化。

## 视觉系统

参考方向：Endel 的视觉不是装饰性插画，而是和声音状态同步的生成式环境。它使用低干扰、持续变化的抽象图形，让用户感到系统正在根据时间、环境和身体状态实时生成声景。《世界皮肤》应参考这种"状态可视化"逻辑，但不要照搬冥想/睡眠 App 的疗愈感；本项目的视觉必须始终回到城市表面、身体停留和地图皮肤。

视觉核心：

- 地图不是背景，而是被感知数据覆盖、腐蚀、沉积的表面。
- 动效不是吸引注意力，而是让用户感到环境正在被读取。
- 图形不是图标说明，而是由声音、移动、时间和记录密度生成的纹理。
- 整体保持低信息密度、慢速变化、深色基底、局部发光。

### 视觉基调

整体应介于"地图层"和"生物膜"之间。

- 黑色或近黑色作为主基底，降低普通地图的功能感。
- 使用低饱和色，不做明亮渐变和疗愈插画。
- 纹理以细颗粒、雾状扩散、细线裂隙、微弱脉冲为主。
- 亮度代表感知强度，不代表好坏、热度或推荐程度。
- 所有可视化都应像缓慢沉积出来，而不是像即时打点。

### 状态层

参考 Endel 的 soundscape/scenario 逻辑，World Skin 可将视觉拆成几种"地图状态层"。这些状态不是模式按钮，而是系统根据数据自动显现的皮肤状态。

#### Ambient / 环境底层

默认存在的低强度皮肤层。

- 表现当前位置附近的基础氛围。
- 未记录区域仍保留暗面和空洞感，不显示为普通空白。
- 动效极慢，只保留呼吸式透明度变化（无颗粒漂移/jitter）。

#### Contact / 接触层

用户长按采样时出现。

- 以当前位置为中心产生收缩和扩散。
- 图形应像手机正在贴近地表读取纹理。
- 声音输入越强，边缘越粗糙；波动越大，轮廓越不稳定。
- 采样未完成前不生成正式地图痕迹，只显示临时接触状态。

#### Imprint / 痕迹层

采样完成后写入地图。

- 记录点不应只是圆点，而是局部沉积。
- 停留记录偏向凝聚、厚、向内压实。
- 穿行记录偏向拉伸、薄、有方向性。
- 重复记录形成层叠，不简单叠加为更亮的热力图。

#### Boundary / 边界层

当相邻区域状态差异明显时出现。

- 使用细裂缝、断续线、错位纹理表现感知边界。
- 边界不应像行政区线，也不应像导航路线。
- 它应像皮肤上的缝，提示用户沿差异移动。

#### Void / 空洞层

用于表现"未被触摸"的区域。

- 空洞不是透明，不是未加载。
- 空洞应有低亮度、低密度、近乎静止的视觉状态。
- 当空洞周围记录变多时，空洞边缘可以变得更清晰，形成探索张力。

### 视觉变量映射

所有视觉变化必须来自数据，避免纯装饰。当前实现采用 v2 感知轴分工：

- `spaciousness`：主控平面扩散 / 收缩与点半径；空旷更展开、更薄，密集更凝聚。
- `gravity`：主控 alpha 与 height；轻盈更低更薄，沉重更高更厚。
- `tension`：主控点阵内部微差幅度；松开时相邻小球明暗和高低差更大，绷紧时更均质。
- `flow`：主控高度线终点偏移、方向一致性和轻微时间残留；不画箭头、轨迹、风向线，不拉伸点阵。
- `soundVector`：接入环境激活、起伏、尖锐度、连续性与颗粒感；loudness 微调 strength，turbulence 微调 flux/contrast，sharpness 进入 edgeHardness，continuity 调整沉积连续性，texture 形成静态 grain。
- `finalColor`：由数据显影强度、spaciousness、gravity、tension 与 soundVector 综合合成；颜色只负责低饱和状态气质，不成为独立主控轴。flow 不参与颜色，只保留高度线方向结构作用。
- 旧 `textureModifier`：仅保留读取兼容；新记录不再写入，也不再作为干湿 / 冷热独立材料系统。

基础数据仍参与底层场：

- 声音平均值 / peak：基础强度、沉积亮度和高度。
- 声音波动性 / movement：flux、局部 contrast、boundary 微调。
- 记录次数：层叠复杂度，而不是简单热度亮度。
- 时间段：色温、透明度、时间切片权重。

### 色彩规则

当前阶段建议保持接近单色，但允许非常克制的状态色。

- 基底：黑色、近黑、深灰。
- 基础纹理：白、灰白、冷灰。
- 夜间记录：偏蓝灰或银灰。
- 白天记录：偏暖灰，但不能变成黄色地图。
- 高波动区域：允许短暂暗红或锈色提示。
- 空洞区域：低亮度蓝黑或灰黑。
- v0.3 扩展色板（感知轴组合色）：blueBlack / mossTeal / stoneBlue / indigoNight / oliveAsh / dustGray / concreteBlue / hazeWhite / warmEarth / flowSlate，仅在轴组合强度足够时显现，不做全局铺色。

禁止：

- 大面积彩虹热力图。
- 高饱和霓虹渐变。
- 像睡眠/冥想 App 一样柔软甜美的插画。
- 用颜色表达"好/坏/推荐/危险"的评价判断。

### 动效规则

动效应像环境自发变化，而不是 UI 表演。

- 默认动画速度要慢。
- 采样时允许响应增强，但必须保持圆滑和连续。
- 记录写入时使用短暂扩散，然后逐渐稳定。
- 地图切换或视图切换不做强烈转场。
- 动效必须服务于"正在读取、正在沉积、正在显影"的感觉。

### UI 与地图关系

界面控件必须让位于地图皮肤。

- 主地图界面必须完全视觉化，不出现任何文字、数字、标签、点位标记或说明。
- 主界面优先显示地图或采样状态，不做卡片堆叠。
- 控件应小、安静、固定，不遮挡关键纹理。
- 文本只解释必要状态，不解释项目概念。
- 个人档案视图可以显示统计，但视觉上仍应像皮肤剖面，而不是数据仪表盘。

### 网格纹理地图层技术实现

地图界面的目标调整为：世界表面覆盖一整层规则分布的点状网格纹理。数据不直接显示为记录点，而是改变网格节点的状态。单条记录影响周围一片区域的节点，多条记录叠加后形成可读的区域倾向。

功能拆分：

- World Map：显示公共/预设聚合数据 + 个人记录叠加；预设数据来自 `data/world_data.js`（`WORLD_SKIN_DATA.records`）；`activeRecords()` 在 world 视图返回 worldRecords + personalRecords。
- My Radius：只显示本机用户个人记录（`personalRecords`）；`activeRecords()` 在 radius 视图只返回 personalRecords。
- Sense：只负责现场采样与主观词选择，写入 personalRecords，不直接写入 World Map。
- 预设数据必须放在独立数据文件中，不嵌入主文件。
- 预设数据必须依附真实地图结构生成，不能在测试半径内均匀随机撒点。
- 地图底层使用 MapLibre GL 作为地图引擎；Canvas 只负责 World Skin 纹理覆盖层。
- 整体视觉：暗色航拍地图、顶部 topbar、底部 footer-nav + 24H 时间轴、右上 utility 按钮、固定点阵和冷暖数据光场。

实现规则：

- 不使用 marker、pin 或独立记录点。
- 真实地图引擎负责经纬度投影、拖拽平移、滚轮缩放、触摸缩放、双指旋转、右键/触控板旋转和键盘导航。
- Canvas 点阵必须使用地图引擎的 `project` 结果，而不是独立手写投影。
- 真实地图瓦片当前使用 CartoCDN voyager_nolabels，不显示地名、POI 或普通导航地图标签。
- 建立覆盖全地图的固定网格节点，每个节点都是世界表面的一个采样点。
- 当前阶段点阵仍保持规则网格，但 active 区域使用静态 height 与高度线表现沉积厚度。
- 地图文字默认不显示；World Map 与 My Radius 都只保留点阵、真实地图边缘和必要控制 UI。
- 地图道路、建筑、绿地和水系边缘必须来自真实航拍底图，避免手绘边界和真实地图不一致。
- 地图边缘可见度按缩放层级调整：区域层较淡，街区层更清楚。
- 点状纹理密度必须克制，优先保证真实地图边缘可读。
- 点阵本体保持规则网格；当前稳定性能版允许数据激活区域产生受限的半径、透明度、低饱和色温、静态高度差异和高度线终点微偏移。
- 点阵必须是稳定的屏幕二维规则网格；地图缩放、旋转、平移时，记录场源使用 MapLibre `project([lng, lat])` 重新投影，影响区域跟随地图移动。
- 覆盖层必须保留清晰动态范围：空洞区、低强度生活底层、普通记录沉积、车站/通勤高密度区需要肉眼可分。
- 未被记录影响的节点必须保持极暗空洞点，不允许用全局平均强度铺成均匀灰网格。
- 地图中心需要显示当前所在位置和方向；无定位时回退到当前地图功能的中心点。
- 每条记录作为场源影响周围节点，而不是被单独绘制。
- 节点变化必须受限：radius、alpha、height 和高度线偏移都必须保持克制，不能退化为柱状图、风向图或热力图。
- active 区域允许静态变亮、变厚和轻微隆起；void 区域必须保持暗、小、低矮。
- 声音平均值控制节点影响范围和基础亮度；spaciousness 与数据强度可以对点半径做小幅偏置。
- 声音波动性控制局部对比，不直接驱动点的快速运动。
- 移动方向只用于 flow 的 screen-space 聚合方向，并轻微偏移已有高度线终点；圆本体不脱离规则网格形成轨迹。
- 附近环境变化需要被放大为局部对比：相邻节点的强度/波动差越大，点阵越容易形成可读的差异带和裂感。
- alpha 计算使用非线性动态范围，避免线性平均造成全屏灰雾：
  - `baseVoidAlpha` 约 `0.012-0.03`。
  - active 区域可进入 `0.16-0.34`。
  - dense 区域可进入 `0.36-0.58`，但不得过曝成白块。
  - `strength < 0.025` 的节点只绘制暗空洞点。
- 高斯影响需要设置 cutoff，低于阈值的远距离影响不参与累加，避免整张地图被抹平。
- sigma 随 zoom 控制在约 `55-150px` 范围内，不超过 `180px`。
- 主观词不直接显示在地图上。
- 主观词写入 `senseVector`，并通过 field 聚合后参与结构微调和 finalColor 合成；旧 `textureModifier` 不再作为新记录主逻辑。
- 当前渲染管线：`recordBaseVisual(record)` 计算基础参数与 v2 轴 fallback，`buildGeoGridField()` 聚合到 cell，`drawGridTexture()` 从 cell 读取最终参数并调用 `getDotAlpha(params)` / `getDotColor(params)`。

当前颜色通道：`data strength + spaciousness + gravity + tension + soundVector → finalColor`。颜色限制在灰化冷暖色皮肤范围内，只作为状态气质；flow 方向不影响颜色。

性能规则：

- 默认状态下不持续逐帧重算数据场。
- 不使用 micro jitter、随机抖动、逐点 pulse、逐点实时位移或逐点 glow。
- 不使用 `gridNodes × allRecords` 的每帧暴力计算。
- 只有以下情况会重算场：地图 pan / zoom / rotate / pitch、时间轴 hour 改变、数据源切换、新记录写入、canvas resize。
- 当前实现使用米制 spatial hash，记录按 `cellSize` 分桶，网格点只查询附近 cells。
- 时间轴变化时计算 target field，并在约 420ms 内插值到新场；不拖动时间轴时不自动播放。
- `requestAnimationFrame` 只用于按需绘制、Sense 采样中绘制和时间场插值，不再无条件常驻重绘。
- 当前性能参数：`gridSpacing:14, cellSize:128, maxInfluenceDistance:260, maxVisibleRecords:2500, targetFPS:30, renderDebounceMs:60`。

预设聚合数据：

- 数据文件：`test0.3/data/world_data.js`。
- 当前版本：`kokubunji-sim-v4-clean`。
- 默认中心：国分寺附近 `{ lat: 35.7000, lng: 139.4800 }`；代码常量 `WORLD_ORIGIN = { lat:35.7000, lng:139.4808 }`。
- 范围：约 8km（`RADIUS_METERS` 来自 `WORLD_SKIN_DATA?.radiusMeters || 5000`）。
- 时间：连续 10 天。
- 用户数：200 人。
- 记录数：原始约 2700 条；`maxVisibleRecords` 上限 2500 条（性能 cap）。
- 数据是模拟公共聚合使用数据，不是个人数据。
- `World Map` 读取 `WORLD_SKIN_DATA.records`；`My Radius` 仍然只读取本机 `localStorage`。
- HTML 使用普通 `<script src="data/world_data.js">` 加载；`world_data.js` 需要提供 `window.WORLD_SKIN_DATA`，不能只写 ES module `export`。

结构化生成规则：

- 车站/通勤节点约 28%：国分寺駅、西国分寺駅、恋ヶ窪駅、一橋学園駅。早 7-9 点与晚 17-20 点权重高，`passing / slow` 较多，`ざらつく / 詰まる / 遠い` 权重高。
- 道路/铁路走廊约 22%：中央線、西武国分寺線、府中街道方向、国分寺南北生活动线。沿折线 corridor 采样并加入横向偏移，形成松散带状记录，不生成完美直线。
- 学校/公共设施约 14%：武蔵野美術大学、東京経済大学周边。白天权重高，`still / slow` 较多，噪声中等，`重い / ほどける / 眠い / ざらつく` 权重高。
- 公园/绿地/水系约 12%：都立武蔵国分寺公園、殿ヶ谷戸庭園、野川・小金井方向绿地边缘。记录较分散，低噪声、低到中等波动，周末略增，`浮く / ほどける / 眠い / 遠い` 权重高。
- 住宅区约 18%：南町・東元町、戸倉・並木町等住宅肌理。低密度、分散、早晚较多，`眠い / 遠い / 乾く / ほどける` 权重高。
- 孤岛记录约 6%：低记录区中的偶发停留或经过，影响周围点阵但不显示为 marker。
- 空洞区域通过 `WORLD_SKIN_VOIDS` 排除，保留走廊夹缝、住宅深处和绿地内部的低记录区域，用于测试 Void 层。

地图交互：

- World Map 和 My Radius 都支持拖拽平移。
- 支持滚轮缩放和移动端双指缩放。
- 支持 Google Maps 风格的旋转/转向：触控双指旋转、触控板/鼠标右键旋转，以及 MapLibre 默认键盘操作。
- 缩放层级自动切换（五档，连续插值）：
  - `region`：广域尺度（zoom < 12.4），点阵间距最大，底图最暗。
  - `area`：区域尺度（zoom 12.4–13.6），默认显示层（zoom ≈13）。
  - `neighborhood`：街区尺度（zoom 13.6–15.0），底图结构更清楚。
  - `street`：近距离尺度（zoom 15.0–16.0），屏幕点阵略密，底图细节清楚。
  - `detail`：最近尺度（zoom ≥ 16.0），高度线长度变化最大，点阵沉积最厚。
- 缩放范围有限制：minZoom:11，maxZoom:17。
- 重置按钮（键盘 `R`）同时恢复平移和缩放。
- 地图交互优先由 MapLibre GL 接管；如果地图引擎加载失败，退回本地 Canvas 投影与手势预览。
- Sense 采样和音波只能在 Sense 页面触发；切换到 World 或 Radius 时必须取消采样并停止绘制音波。

显示阈值：

- 所有视觉参数必须有硬上限，无论数据量如何叠加都不能超过阈值。
- 当前原型阈值（`VISUAL_LIMITS`）：
  - `strength <= 0.72`
  - `flux <= 0.72`
  - `contrast <= 0.48`
  - `height <= 92`
  - `radius <= 2.4`
  - `alpha <= 0.72`
  - `boundaryAlpha <= 0.32`
- 新增视觉变量时必须先定义阈值，再接入渲染。
- Debug View：键盘 `D` 临时切换，检查记录数量、屏幕参与记录、strength/contrast/alpha 的 min/max/avg、sigma、spacing、zoom 等。
- Map Debug：键盘 `M` 临时切换地图底层 dark / balanced / bright 三档，默认 `balanced`。
- 测试数据：键盘 `T` 切换 testdata1，`Y` 切换 testdata2，再次按下还原。

时间控制系统：

- World Map 显示 24 小时时间轴，时间滑块范围 -12h 到现在。
- 用户滑动时间轴时，地图显示对应小时附近的感知场变化。
- 视觉场多窗口综合叠加（`state.aggregation`）：
  - hour 0.44 / day 0.24 / threeDay 0.16 / week 0.11 / month 0.05。
- 当前基准日期：2026-05-04（`Math.floor((date.getTime() - Date.UTC(2026,4,4)) / 86400000)`）。
- 时间场切换动画约 420ms 插值。
- 地图底层颜色（brightness / saturation / overlay）随时间轴小时值平滑变化；`getMapVisualByHour()` 按 dawn/day/dusk/night/deepNight 五段处理。

后续推荐升级：

- 将网格影响计算移到离屏标量场或 WebGL，避免每次 resize 后 CPU 重算过重。
- 将网格节点分为多层：真实地图边缘层、水平点阵纹理层、数据透明度/色温层。
- 后续如果继续强化 3D，应迁移到 WebGL / Three.js 或 MapLibre custom layer，将网格作为真实 mesh，以顶点高度表达感知场。
- 使用真实航拍瓦片或矢量路网时，只保留空间边缘，不引入 POI、评分或导航 UI。

### 采样呼吸引导

10 秒采样不是普通进度条，而是让用户感受周围环境的短暂停留。

实现规则：

- 采样按钮不显示倒计时数字。
- 使用多圆环（`ringCount:7`，`baseRadius:68px`，步长 `14px`）进行呼吸式放大和缩小，节奏必须肉眼可见。
- 当前呼吸分三阶段：扩张 4s（growMs:4000）→ 悬浮 2s（floatMs:2000）→ 收缩 4s（shrinkMs:4000），合计 10s。
- 扩张阶段：scale 从 minScale 到 maxScale，easeInOutCubic；opacity 0.72 → 0.98。
- 悬浮阶段：scale 做小幅 sine 抖动（±2.5%），阶段切换时触发震动。
- 收缩阶段：scale 从 1 到 0.035，opacity 幂次衰减；结束后触发 `completeRecording()`。
- Web 端使用 Vibration API 做轻微触觉提示；不支持震动时直接忽略。
- 引导以视觉为主：sense 视图有 hint-main「押しつづける」/ hint-sub「10秒のあいだ」。

### 采样后词语选择

采样完成后不显示数据表格。

实现规则：

- 采样完成 → release 阶段 600ms 延迟 → 打开 recordModal（selecting 阶段）。
- 当前实现：使用 `SENSE_PROTOTYPE_ROUNDS` 固定词集（[浮く / 詰まる / 流れる] 和 [重い / 乾く / ほどける]）；词组按轮次显示，不从 v2 库随机选（`pickRoundWords()` 已实现但当前用 prototype rounds）。
- `SENSE_ROUNDS = 2`，两轮各选一词；选词后聚合为 `senseVector`（`aggregateSV()`）。
- 词的屏幕位置由 `SENSE_LABEL_BASE_LAYOUT` 定义随机范围，每次展示随机分布，有 animation delay。
- 用户选词 → `onWordSelected()` → 若还有下一轮则继续，否则 `finalizeSense()`。
- `finalizeSense()` 后 1100ms 调用 `savePending()`，保存记录并切换到 radius 视图。
- 词语只在选择阶段出现，不留在地图上。

### 与 Endel 的差异

可以参考：

- 生成式抽象视觉。
- 根据环境和身体输入实时变化。
- 低干扰、沉浸式、少文本。
- 用不同状态组织体验。

必须避免：

- 把 World Skin 做成放松、睡眠、专注工具。
- 把地图变成纯全屏抽象动画。
- 使用过度圆润、疗愈、品牌化的视觉语言。
- 让视觉脱离地理位置和真实空间。

## 交互原则

### 慢

长按、等待、缓慢显现、延迟更新都是感知条件，不是低效。

### 少

界面不提供过多信息，不显示推荐列表、排行榜、用户热度或"附近最值得记录地点"。

### 现场性

只有在现场才能记录。远程不能写入地点，也不能事后补记。

### 痕迹感

每一次记录都必须留下可见痕迹，即使很小。用户需要感到自己的身体确实改变了地图表面。

## 禁止项

项目不得加入以下机制：

- 五星评分、好评率、推荐指数。
- 热门区域、贡献榜、Top 用户。
- 连续打卡、徽章、积分、等级。
- 主动生成社交媒体分享海报。
- 将多人数据做成热度图或排行榜。

## 当前代码原型状态

主文件：`test0.3/` 目录（`app.js` + `index.html` + `styles.css`）

数据文件目录：`test0.3/data/`（`world_data.js`、`my_data.js`、`testdata.js`、`testdata2.js`）

存储键：`STORAGE_KEY = "world-skin-v03-records"`；种入标记 `SEED_KEY = "world-skin-v03-seeded-r1"`

说明：`test0.3/` 是在 `test0.2.html` 之后重新拆分的多文件目录结构；UI 由 Figma 1:1 实现，不沿用 test0.2 界面结构。

已实现：

- 手机端多文件 Web 原型，独立 JS / CSS / data 文件。
- MapLibre GL 真实地图引擎（v4.7.1）+ Canvas 纹理覆盖层。
- 启动屏（launchScreen）：SVG 针上升动画 → 大圆 + KOKO wordmark 展开；地图加载完成后最少持续 2.5s 后淡出。
- 状态机：world / radius / sense / profile / records / settings 六个视图，通过 `app.dataset.view` + `app.classList` 驱动。
- 导航状态：nav-closed（默认）/ nav-open；avatar 按钮 nav-closed 时触发 nav-open，nav-open 时进入 profile 视图。
- 底部 footer-nav：三个按钮（world / sense / radius），SVG 图标，nav-open 时显示，nav-closed 时收起。
- Profile 视图：大头像（234×234px）、用户名、记录件数、今の傾向（top 3 词）、最近の記録、设置 / 分享按钮。
- Records 视图：可滚动统计面板；recSubtitle 显示活动半径（哈弗辛距离，`约Xkm` / `约Xm`）；时间段点阵图（朝/昼/夕/夜）；感觉词云（Figma 坐标布局，12 词）。
- Settings 视图：静态列表（显示 / 记録 / 通知三组），内容未接入功能。
- Sense 视图：Figma 层结构（bg / bottom-mask / mask / mapcore / effect / core），press-target 长按按钮，back 按钮。
- Sense 阶段：`idle → contact（900ms）→ diffusion（1200ms 后）→ ready → recording → release → selecting → complete`。
- 10 秒呼吸采样（4s 扩张 + 2s 悬浮 + 4s 收缩）；7 环，baseRadius:68px，步长 14px。
- 麦克风采样；权限拒绝后不报错，下次按下自动重试。
- 移动状态粗判：still / slow / passing（基于路径哈弗辛距离与速度）。
- soundVector 计算：spectralSharpness / spectralSpread 从 FFT 频率数据提取。
- v2 感知词库 32 词（`SENSE_WORD_LIST`），每词含 sv 和 tex 向量。
- 词选择后聚合 `senseVector`；`savePending()` 后自动切换到 radius 视图。
- World Map：`activeRecords()` = worldRecords + personalRecords，地图中心 `DEFAULT_VIEW`。
- My Radius：`activeRecords()` = 仅 personalRecords；地图自动定位到个人中心。
- 个人记录从 `data/my_data.js`（`MY_SKIN_DATA`）种入，SEED_KEY 防重复。
- `localStorage` 持久化（STORAGE_KEY）。
- 时间轴（time-axis）：范围 -12h 到现在；拖动时场过渡 420ms 插值。
- 地图时间视觉：按小时平滑切换 dawn / day / dusk / night / deepNight 五态。
- 地图视觉模式：dark / balanced / bright 三档（键盘 M 切换）。
- 缩放层级五档连续插值；地图视图范围：minZoom:11，maxZoom:17。
- 当前位置标记：水滴形（teardrop）白色，带 pitch 自适应形状和 halo 脉冲。
- 当前位置获取；失败时回退国分寺测试半径并 toast 提示。
- Debug View（键盘 D）；测试数据集切换（键盘 T / Y）。
- v2 感知轴 → 颜色通道映射，10 种组合色（组合强度驱动，不做全局铺色）。

主要差距：

- 文件仍为单页原型，尚未拆分 ES 模块。
- Profile 视图的「今の傾向」时段（pvTrendTime）目前输出 "—"，未接入时段统计。
- Profile 视图的「最近の記録」area 字段目前 fallback 为 "国分寺駅周辺"，未实现地名反查。
- Sense 视图词选择仍使用 `SENSE_PROTOTYPE_ROUNDS` 固定词集（非全库随机）。
- 孤岛识别尚未单独可视化。
- 地点级时间剖面仍是第一版统计，尚未支持单个地点的完整多时段对照。
- Settings 视图内容静态，未接入任何功能。
- Share（分享）按钮存在于 profile 视图，但分享功能尚未实现。
- 当前真实地图底层来自 raster tile；如需对道路/地块边缘做独立算法控制，下一步应切换到矢量瓦片或 MapLibre vector source。

## 原型阶段验收目标

当前阶段先集中在小范围生活圈。`test0.3/` 当前测试区域为国分寺周边约 5–8km。

最低可验证目标：

1. 手机浏览器可打开，启动屏正常播放并淡出。
2. 可获取当前位置；失败时回退并提示。
3. 用户可长按 10 秒完成一次记录；呼吸动画肉眼可见。
4. 可采集基础环境音量与时间信息；权限失败时不崩溃。
5. 可在地图上显示记录痕迹（个人记录影响网格纹理）。
6. 可看到"我的半径"（Records 视图）中的已记录区域与空白区域。
7. Profile 视图可正常打开，显示记录件数和词倾向。
8. 导航状态机：nav-closed / nav-open / profile / records / settings 之间的切换符合状态机规格。

## 更新规则

- 当前实现以 `MAP_LOG` 的最新变更记录、`UI_skill/` 的实装规则和 `test0.3/app.js` 的代码为准。
- `MAP_SPEC` 记录可执行规格，随着实现持续更新。
- `TODO` 记录当前进度和下一步。
- `MAP_LOG` 记录每次重要修改、决策和偏离既定规格的原因。

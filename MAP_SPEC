# MAP_SPEC

更新时间：2026-05-13
依据：`guide book`：《世界皮肤 / World Skin》项目说明书 v0.2。

## 项目定位

项目名：世界皮肤 / World Skin

项目形式：手机端 Web 原型 / 感知型地图系统 / 城市体验可视化实验

一句话说明：

《世界皮肤》是一套以身体感知为基础的地图记录系统。它不使用评分、推荐、排行榜或打卡图像来理解城市，而是让用户在具体地点停留片刻，通过声音、移动状态、时间与个人感受，将日常空间转译为一张具有纹理、噪点、厚度与空洞的“皮肤”。

## 核心问题

本项目不是为了做一个更方便的地图，而是回答：

当地图不再以消费判断为中心，而以身体经验为中心时，它会变成什么？

传统地图关注路线、店铺、坐标、评分和热度。《世界皮肤》关注粗糙度、密度、波动、空白、边界和痕迹。

## 设计目标

### 从评价地图转向感知地图

- 不使用星级、点赞、排行榜、热门地点等指标。
- 不判断地点好坏，只记录地点的强度和感知状态。
- 地点不作为消费对象呈现，而作为身体经验的沉积面呈现。

### 从打卡行为转向身体停留

- 记录必须发生在现场。
- 用户需要长按约 10 秒，形成短暂的停留。
- 记录不是“我到过这里”，而是“我在这里停留并感受过”。

### 从地点集合转向强度场

- 单个地点不是孤立点，而是皮肤上的局部纹理。
- 多次记录后，生活圈逐渐形成由噪声、时间、移动状态和个人痕迹构成的强度场。

## 核心功能

### 体感记录

用户在实际地点长按 10 秒，完成一次身体性记录。

记录内容包括：

- 环境声音：平均音量、音量起伏、噪声峰值、持续嘈杂或安静状态。
- 位置与移动：停留、缓慢移动、快速穿过。
- 时间：早晨、白天、夜晚、平日、周末，后续可扩展天气。
- 简短主观标记：例如浮く、重い、乾く、ざらつく、眠い、遠い、詰まる、ほどける。

当前代码原型已实现：

- 麦克风音量采样。
- 麦克风不可用时模拟噪波。
- 10 秒长按记录。
- 时间段描述。
- 简短标签选择，但当前标签仍是「沉 / 躁 / 空 / 绷」，需要按 guide book 调整。

### 我的半径

以用户日常活动范围为基础生成个人感知范围。

它不是社交生活圈，也不是通勤效率圈，而是被记录逐渐显影的身体经验范围。

需要呈现：

- 哪些路经常经过。
- 哪些地方已经形成纹理。
- 哪些地方仍然空白。
- 哪些区域只有白天记录，没有夜晚记录。
- 哪些空间明明很近，却从未被真正感知。

### 空洞与孤岛

空洞：

- 周围已有较多记录，但中间仍然空白的区域。
- 用于提示用户可能长期绕开了某个地方。

孤岛：

- 大片空白中出现的孤立记录点。
- 用于提示某次偶然经过形成的异常痕迹。

### 边界带

当相邻区域呈现明显不同的皮肤状态时，显示感知边界。

边界带不是路线推荐，而是探索提示：沿着差异移动，观察城市在哪里发生了变化。

### 时间剖面

同一地点在不同时间段具有不同皮肤。

需要支持对比：

- 早晨 / 午后 / 夜晚。
- 平日 / 周末。
- 晴天 / 雨天（后续）。
- 同一地点的沉默、活跃、空洞和波动变化。

## 视觉系统

参考方向：Endel 的视觉不是装饰性插画，而是和声音状态同步的生成式环境。它使用低干扰、持续变化的抽象图形，让用户感到系统正在根据时间、环境和身体状态实时生成声景。《世界皮肤》应参考这种“状态可视化”逻辑，但不要照搬冥想/睡眠 App 的疗愈感；本项目的视觉必须始终回到城市表面、身体停留和地图皮肤。

视觉核心：

- 地图不是背景，而是被感知数据覆盖、腐蚀、沉积的表面。
- 动效不是吸引注意力，而是让用户感到环境正在被读取。
- 图形不是图标说明，而是由声音、移动、时间和记录密度生成的纹理。
- 整体保持低信息密度、慢速变化、深色基底、局部发光。

### 视觉基调

整体应介于“地图层”和“生物膜”之间。

- 黑色或近黑色作为主基底，降低普通地图的功能感。
- 使用低饱和色，不做明亮渐变和疗愈插画。
- 纹理以细颗粒、雾状扩散、细线裂隙、微弱脉冲为主。
- 亮度代表感知强度，不代表好坏、热度或推荐程度。
- 所有可视化都应像缓慢沉积出来，而不是像即时打点。

### 状态层

参考 Endel 的 soundscape/scenario 逻辑，World Skin 可将视觉拆成几种“地图状态层”。这些状态不是模式按钮，而是系统根据数据自动显现的皮肤状态。

#### Ambient / 环境底层

默认存在的低强度皮肤层。

- 表现当前位置附近的基础氛围。
- 未记录区域仍保留暗面和空洞感，不显示为普通空白。
- 动效极慢，只保留呼吸式透明度变化（无颗粒漂移/jitter）。

#### Contact / 接触层

用户长按采样时出现。

- 以当前位置为中心产生收缩和扩散。
- 图形应像手机正在贴近地表读取纹理。
- 声音输入越强，边缘越粗糙；波动越大，轮廓越不稳定。
- 采样未完成前不生成正式地图痕迹，只显示临时接触状态。

#### Imprint / 痕迹层

采样完成后写入地图。

- 记录点不应只是圆点，而是局部沉积。
- 停留记录偏向凝聚、厚、向内压实。
- 穿行记录偏向拉伸、薄、有方向性。
- 重复记录形成层叠，不简单叠加为更亮的热力图。

#### Boundary / 边界层

当相邻区域状态差异明显时出现。

- 使用细裂缝、断续线、错位纹理表现感知边界。
- 边界不应像行政区线，也不应像导航路线。
- 它应像皮肤上的缝，提示用户沿差异移动。

#### Void / 空洞层

用于表现“未被触摸”的区域。

- 空洞不是透明，不是未加载。
- 空洞应有低亮度、低密度、近乎静止的视觉状态。
- 当空洞周围记录变多时，空洞边缘可以变得更清晰，形成探索张力。

### 视觉变量映射

所有视觉变化必须来自数据，避免纯装饰。

- 声音平均值：颗粒密度、区域厚度、基础亮度。
- 声音波动性：边缘粗糙度、轮廓抖动、局部裂隙。
- 突发噪声峰值：短暂尖锐闪烁或局部破裂，但不能刺眼。
- 停留时间：沉积厚度、中心凝聚度。
- 移动方向：纹理拉伸方向、尾迹长度。
- 记录次数：层叠复杂度，而不是简单热度亮度。
- 时间段：色温、透明度、纹理松紧。

### 色彩规则

当前阶段建议保持接近单色，但允许非常克制的状态色。

- 基底：黑色、近黑、深灰。
- 基础纹理：白、灰白、冷灰。
- 夜间记录：偏蓝灰或银灰。
- 白天记录：偏暖灰，但不能变成黄色地图。
- 高波动区域：允许短暂暗红或锈色提示。
- 空洞区域：低亮度蓝黑或灰黑。

禁止：

- 大面积彩虹热力图。
- 高饱和霓虹渐变。
- 像睡眠/冥想 App 一样柔软甜美的插画。
- 用颜色表达“好/坏/推荐/危险”的评价判断。

### 动效规则

动效应像环境自发变化，而不是 UI 表演。

- 默认动画速度要慢。
- 采样时允许响应增强，但必须保持圆滑和连续。
- 记录写入时使用短暂扩散，然后逐渐稳定。
- 地图切换或视图切换不做强烈转场。
- 动效必须服务于“正在读取、正在沉积、正在显影”的感觉。

### UI 与地图关系

界面控件必须让位于地图皮肤。

- 主地图界面必须完全视觉化，不出现任何文字、数字、标签、点位标记或说明。
- 主界面优先显示地图或采样状态，不做卡片堆叠。
- 控件应小、安静、固定，不遮挡关键纹理。
- 文本只解释必要状态，不解释项目概念。
- 个人档案视图可以显示统计，但视觉上仍应像皮肤剖面，而不是数据仪表盘。

### 网格纹理地图层技术实现

地图界面的目标调整为：世界表面覆盖一整层规则分布的点状网格纹理。数据不直接显示为记录点，而是改变网格节点的状态。单条记录影响周围一片区域的节点，多条记录叠加后形成可读的区域倾向。

功能拆分：

- World Map：显示公共/预设聚合数据，当前原型使用“100 个用户在国分寺周边 5km 内连续使用一周”的模拟结果。
- My Radius：只显示本机用户自己的记录与统计。
- Sense：只负责现场采样与主观词选择，写入 My Radius，不直接写入 World Map。
- 预设数据必须放在独立数据文件中，不嵌入主原型文件。
- 预设数据必须依附真实地图结构生成，不能在 5km 圆形范围内均匀随机撒点。
- 地图底层需要接入真实地图引擎；当前原型使用 MapLibre GL 作为地图引擎，Canvas 只负责 World Skin 纹理覆盖层。
- 整体视觉参考当前总图：暗色航拍地图、左侧竖向导航、底部 24H 时间轴、右上地图控制、固定点阵和冷暖数据光场。

实现规则：

- 不使用 marker、pin 或独立记录点。
- 真实地图引擎负责经纬度投影、拖拽平移、滚轮缩放、触摸缩放、双指旋转、右键/触控板旋转和键盘导航。
- Canvas 点阵必须使用地图引擎的 `project` 结果，而不是独立手写投影。
- 真实地图瓦片当前使用正航拍图，不显示地名、POI 或普通导航地图标签。
- 建立覆盖全地图的固定网格节点，每个节点都是世界表面的一个采样点。
- 当前阶段将点阵纹理回退为水平二维覆盖层，暂时不做 pseudo-3D 隆起。
- 地图文字默认不显示；World Map 与 My Radius 都只保留点阵、真实地图边缘和必要控制 UI。
- 地图道路、建筑、绿地和水系边缘必须来自真实航拍底图，避免手绘边界和真实地图不一致。
- 地图边缘可见度按缩放层级调整：区域层较淡，街区层更清楚。
- 点状纹理密度必须克制，优先保证真实地图边缘可读。
- 点阵本体保持规则网格；当前稳定性能版允许数据激活区域产生受限的半径、透明度、低饱和色温和静态高度差异。
- 点阵必须是稳定的屏幕二维规则网格；地图缩放、旋转、平移时，记录场源使用 MapLibre `project([lng, lat])` 重新投影，影响区域跟随地图移动。
- 覆盖层必须保留清晰动态范围：空洞区、低强度生活底层、普通记录沉积、车站/通勤高密度区需要肉眼可分。
- 未被记录影响的节点必须保持极暗空洞点，不允许用全局平均强度铺成均匀灰网格。
- 地图中心需要显示当前所在位置和方向；无定位时回退到当前地图功能的中心点。
- 每条记录作为场源影响周围节点，而不是被单独绘制。
- 节点变化必须受限：radius `0.8-2.4px`，alpha `0.012-0.72`，height 不超过 `96`。
- active 区域允许静态变亮、变厚和轻微隆起；void 区域必须保持暗、小、低矮。
- 声音平均值控制节点影响范围和基础亮度，但最终显示点半径保持固定。
- 声音波动性控制局部对比，不直接驱动点的快速运动。
- 移动方向可以影响场源方向性，但不再让单个点产生可见偏移。
- 附近环境变化需要被放大为局部对比：相邻节点的强度/波动差越大，点阵越容易形成可读的差异带和裂感。
- alpha 计算使用非线性动态范围，避免线性平均造成全屏灰雾：
  - `baseVoidAlpha` 约 `0.012-0.03`。
  - active 区域可进入 `0.16-0.34`。
  - dense 区域可进入 `0.36-0.58`，但不得过曝成白块。
  - `strength < 0.025` 的节点只绘制暗空洞点。
- 高斯影响需要设置 cutoff，低于阈值的远距离影响不参与累加，避免整张地图被抹平。
- sigma 随 zoom 控制在约 `55-150px` 范围内，不超过 `180px`。
- 主观词不直接显示在地图上。
- 主观词目前只存储在记录中，用于 tagGrid 统计；**不影响任何视觉参数**（已回退，待重设计）。
- 当前渲染管线：`recordBaseVisual(record)` 计算基础参数，直接进入 `getDotAlpha(params)` 和 `getDotColor(params)`；无词语偏置层。

**视觉通道重设计留空。** 当前只有：`noise + flux + time-of-day → strength / contrast / warmth / boundary → alpha / radius / cold/neutral/warm color / height`。下一阶段将在此基础上重新定义感知→视觉映射。
- World Map 使用真实地图边缘用于确认空间结构；地名不显示，避免变成普通导航地图。
- My Radius 可以复用同一网格系统，但数据源必须是本机个人记录。

当前原型渲染管线：

1. MapLibre GL 根据真实地图中心、缩放、平移、旋转状态将经纬度转换为屏幕容器坐标。
2. 地图底层使用无文字 raster tile，默认采用 `pitch: 42`、`bearing: -16` 的轻微 3D 斜视视角。
3. 需要绘制时清空 Canvas。
4. 根据当前 MapLibre 视口反投影得到地理范围，在局部米制坐标中生成规则地理网格。
5. 每个网格点转回 `lng / lat` 后使用 MapLibre `project([lng, lat])` 投影到屏幕，因此点阵贴合 pitch / bearing 后的地图表面。
6. 记录先在局部米制坐标中进入 spatial hash，再按附近 cells 查询，用高斯衰减累加影响。
7. 将影响场转换为 `strength / flux / warmth / boundary`。
8. 计算相邻节点差异，得到 `contrast`，用于突出附近环境变化。
9. 根据 `strength / flux / contrast` 计算节点 alpha、色温、radius 和静态 height。
10. 使用 height 产生克制的屏幕上移偏移，模拟皮肤表面的静态起伏；不绘制柱状图，不绘制山峰，不绘制热力渐变。
11. 真实地图道路/地块/水系边缘在点阵下方显示，并随缩放层级增强。
12. World Map 使用预设聚合数据；My Radius 使用 `localStorage` 中的个人数据。

性能规则：

- 默认状态下不持续逐帧重算数据场。
- 不使用 micro jitter、随机抖动、逐点 pulse、逐点实时位移或逐点 glow。
- 不使用 `gridNodes × allRecords` 的每帧暴力计算。
- 只有以下情况会重算场：地图 pan / zoom / rotate / pitch、时间轴 hour 改变、数据源切换、新记录写入、canvas resize。
- 当前实现使用米制 spatial hash，记录按 `cellSize` 分桶，网格点只查询附近 cells。
- 时间轴变化时计算 target field，并在约 420ms 内插值到新场；不拖动时间轴时不自动播放。
- `requestAnimationFrame` 只用于按需绘制、Sense 采样中绘制和时间场插值，不再无条件常驻重绘。

预设聚合数据：

- 数据文件：`world_data.js`。
- 当前版本：`kokubunji-sim-v4-clean`。
- 默认中心：国分寺附近 `{ lat: 35.7000, lng: 139.4800 }`。
- 范围：约 8km。
- 时间：连续 10 天。
- 用户数：200 人。
- 记录数：最多 5000 条（cap）。
- 数据是模拟公共聚合使用数据，不是个人数据。
- `World Map` 读取 `WORLD_SKIN_DATA.records`；`My Radius` 仍然只读取本机 `localStorage`。
- 当前 HTML 使用普通 `<script src="world_data.js"></script>` 加载，因此 `world_data.js` 需要提供 `window.WORLD_SKIN_DATA`，不能只写 ES module `export`。

结构化生成规则：

- 车站/通勤节点约 28%：国分寺駅、西国分寺駅、恋ヶ窪駅、一橋学園駅。早 7-9 点与晚 17-20 点权重高，`passing / slow` 较多，`ざらつく / 詰まる / 遠い` 权重高。
- 道路/铁路走廊约 22%：中央線、西武国分寺線、府中街道方向、国分寺南北生活动线。沿折线 corridor 采样并加入横向偏移，形成松散带状记录，不生成完美直线。
- 学校/公共设施约 14%：武蔵野美術大学、東京経済大学周边。白天权重高，`still / slow` 较多，噪声中等，`重い / ほどける / 眠い / ざらつく` 权重高。
- 公园/绿地/水系约 12%：都立武蔵国分寺公園、殿ヶ谷戸庭園、野川・小金井方向绿地边缘。记录较分散，低噪声、低到中等波动，周末略增，`浮く / ほどける / 眠い / 遠い` 权重高。
- 住宅区约 18%：南町・東元町、戸倉・並木町等住宅肌理。低密度、分散、早晚较多，`眠い / 遠い / 乾く / ほどける` 权重高。
- 孤岛记录约 6%：低记录区中的偶发停留或经过，影响周围点阵但不显示为 marker。
- 空洞区域通过 `WORLD_SKIN_VOIDS` 排除，保留走廊夹缝、住宅深处和绿地内部的低记录区域，用于测试 Void 层。
- 当前版本先手动定义接近真实地图结构的 seed zones / corridor polylines；后续可替换为真实 POI 或 vector tile 派生数据。

地图交互：

- World Map 和 My Radius 都支持拖拽平移。
- 支持滚轮缩放和移动端双指缩放。
- 支持 Google Maps 风格的旋转/转向：触控双指旋转、触控板/鼠标右键旋转，以及 MapLibre 默认键盘操作。
- 缩放层级自动切换（四档，连续插值）：
  - `region`：广域尺度，点阵间距最大，底图最暗。
  - `area`：区域尺度，默认显示层（zoom ≈13）。
  - `neighborhood`：街区尺度，底图结构更清楚。
  - `street`：近距离尺度，屏幕点阵略密，底图细节最清楚。
- 缩放范围需要有限制，避免用户离开测试半径太远。
- 重置按钮同时恢复平移和缩放。
- 地图交互优先由 MapLibre GL 接管；如果地图引擎加载失败，退回本地 Canvas 投影与手势预览。
- Sense 采样和音波只能在 Sense 页面触发；切换到 World 或 Radius 时必须取消采样并停止绘制音波。

显示阈值：

- 所有视觉参数必须有硬上限，无论数据量如何叠加都不能超过阈值。
- 阈值用于防止网格爆亮、过密或真实地图边缘被点阵遮挡。
- 当前原型阈值：
  - `strength <= 0.72`
  - `flux <= 0.72`
  - `contrast <= 0.48`
  - `height <= 92`
  - `radius <= 2.4`
  - `alpha <= 0.72`
  - `boundaryAlpha <= 0.32`
- 新增视觉变量时必须先定义阈值，再接入渲染。
- Debug View：按 `D` 临时切换，用于检查记录数量、屏幕参与记录、strength/contrast/alpha 的 min/max/avg、sigma、spacing、zoom 和 radius max。Debug View 不属于最终 UI。
- Map Debug：按 `M` 临时切换地图底层 `dark / balanced / bright` 三档，默认 `balanced`，用于检查道路、水系、铁路和街区边缘是否可读。

时间控制系统：

- World Map 需要显示 24 小时时间轴，参考 Apple Weather 的时间滑动体验。
- 用户滑动时间轴时，地图显示对应小时附近的感知场变化。
- 视觉场不是只看单小时数据，而是多窗口综合叠加：
  - hour：当前小时附近的短时变化。
  - day：最近一天的趋势。
  - threeDay：近三日的持续倾向。
  - week：一周整体沉积。
  - month：长期底色；当前原型数据只有一周，因此用弱权重近似。
- 当前权重建议：hour 0.44、day 0.24、threeDay 0.16、week 0.11、month 0.05。
- 动画频率必须慢，接近天气图层的缓慢流动，不允许快速粒子抖动。

后续推荐升级：

- 将网格影响计算移到离屏标量场或 WebGL，避免每次 resize 后 CPU 重算过重。
- 将网格节点分为多层：真实地图边缘层、水平点阵纹理层、数据透明度/色温层。
- 后续如果继续强化 3D，应迁移到 WebGL / Three.js 或 MapLibre custom layer，将网格作为真实 mesh，以顶点高度表达感知场。
- 使用真实航拍瓦片或矢量路网时，只保留空间边缘，不引入 POI、评分或导航 UI。

### 采样呼吸引导

10 秒采样不是普通进度条，而是让用户感受周围环境的短暂停留。

实现规则：

- 采样按钮不显示倒计时数字。
- 使用多个白色线条同心圆进行呼吸式放大和缩小，节奏必须肉眼可见。
- 参考 Apple Watch Mindfulness 的交互：吸气时动画扩大，呼气时动画缩小。
- 当前 10 秒原型使用 5 秒吸气 + 5 秒呼气，完成一轮慢呼吸；后续可改为 4 秒吸气 + 6 秒呼气。
- Web 端可使用 Vibration API 做轻微触觉提示；不支持震动时直接忽略。
- 引导以视觉为主，但必须保留一条简短日语提示，帮助用户进入状态。
- 初始提示示例：`静かにして、まわりの世界を感じる`。
- 吸气提示示例：`息を吸って、世界を近くに置く`。
- 呼气提示示例：`息を吐いて、音をほどく`。

### 采样后词语选择

采样完成后不显示数据表格。

实现规则：

- （待重设计：当前为随机两轮选择，无推断逻辑）原设计：系统根据噪声、波动、移动状态和时间段，推断 1 到 3 组候选感知词。
- 每组词以浮现方式出现在屏幕上，用户选择一个即完成写入。
- 如果当前组没有合适的词，用户选择 skip 进入下一组。
- 如果所有组都 skip，则以无主观词写入记录。
- 词语只在选择阶段出现，不留在地图上。

### 与 Endel 的差异

可以参考：

- 生成式抽象视觉。
- 根据环境和身体输入实时变化。
- 低干扰、沉浸式、少文本。
- 用不同状态组织体验。

必须避免：

- 把 World Skin 做成放松、睡眠、专注工具。
- 把地图变成纯全屏抽象动画。
- 使用过度圆润、疗愈、品牌化的视觉语言。
- 让视觉脱离地理位置和真实空间。

## 交互原则

### 慢

长按、等待、缓慢显现、延迟更新都是感知条件，不是低效。

### 少

界面不提供过多信息，不显示推荐列表、排行榜、用户热度或“附近最值得记录地点”。

### 现场性

只有在现场才能记录。远程不能写入地点，也不能事后补记。

### 痕迹感

每一次记录都必须留下可见痕迹，即使很小。用户需要感到自己的身体确实改变了地图表面。

## 禁止项

项目不得加入以下机制：

- 五星评分、好评率、推荐指数。
- 热门区域、贡献榜、Top 用户。
- 连续打卡、徽章、积分、等级。
- 主动生成社交媒体分享海报。
- 将多人数据做成热度图或排行榜。

## 当前代码原型状态

主文件：`test0.2.html`

说明：`test0.2.html` 是无视 `test0.1.html` 后新建的独立原型；当前使用 MapLibre GL 作为真实地图引擎，不沿用旧版界面结构。

已实现：

- 手机端单页 HTML 原型。
- World Map 与 My Radius 已拆成两个功能。
- Canvas 生成式网格点状纹理地图层。
- World Map 默认中心：国分寺附近，半径约 5km。
- World Map 数据来自外部文件 `world_data.js`。
- World Map 当前预设数据：200 个用户在国分寺周边 8km 连续使用 10 天的模拟聚合记录。
- 当前位置获取，失败时回退国分寺测试半径。
- 10 秒长按采样。
- 麦克风采样；不可用时使用模拟噪声。
- 航拍地图边缘根据当前测试区域显示；地名文字默认隐藏。
- 环境底噪、波动性、噪声峰值计算。
- 移动状态粗判：still、slow、passing。
- 不同移动状态对应沉积或方向性痕迹。
- 使用 guide book 中的日语感知词。
- `localStorage` 本地持久化。
- My Radius 默认先展示个人地图；具体信息通过下拉/抽屉按钮展开。
- My Radius 详情面板展示个人记录数量、空洞单元、平均皮肤强度、时间剖面和感知词分布。

主要差距：

- 仍是单文件原型，尚未拆分模块。
- 当前真实地图底层来自 raster tile，边缘与真实地图一致；如需对道路/地块边缘做独立算法控制，下一步应切换到矢量瓦片或 MapLibre vector source。
- 已新增 `BUGS` 文档，用于记录 bug、修复方式和回归检查项。
- 孤岛识别尚未单独可视化。
- 地点级时间剖面仍是第一版统计，尚未支持单个地点的完整多时段对照。
- 权限失败状态已有提示，但还不是完整 UI 状态系统。

## 原型阶段验收目标

当前阶段先集中在小范围生活圈。`test0.2.html` 当前测试区域为国分寺周边约 8km。

最低可验证目标：

1. 手机浏览器可打开。
2. 可获取当前位置。
3. 用户可长按完成一次记录。
4. 可采集基础环境音量与时间信息。
5. 可在地图上显示记录痕迹。
6. 可看到“我的半径”中的已记录区域与空白区域。
7. 至少实现一种探索提示：空洞、边界带或时间剖面。

## 更新规则

- `guide book` 是项目概念与设计原则的最高依据。
- `MAP_SPEC` 记录可执行规格，随着实现持续更新。
- `TODO` 记录当前进度和下一步。
- `MAP_LOG` 记录每次重要修改、决策和偏离 guide book 的原因。

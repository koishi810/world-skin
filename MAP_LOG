# MAP_LOG

## Density Continuity Fix / 2026-05-12

### Problems

- zoom 层级边界处（12.4 / 13.6 / 15.0）点阵密度和 sigma 跳变明显
- 低密度区域（region / area 层）点阵太稀，void 区域几乎不可见
- 现有 getZoomPreset 只在最后 0.28 zoom 范围内插值，过渡太急

### Changes

- `DOT_DENSITY_VISUAL`：targetScreenSpacingPx 15→13.5，minScreenSpacingPx 12→11，maxScreenSpacingPx 18→16，baseRadiusPx 1.15→1.10，minRadiusPx 0.75→0.72，maxRadiusPx 2.25→2.15；字段 maxDots/minDots 改为 maxDotsDesktop(9600)/maxDotsMobile(6800)/minDotsVisible(2600)
- `ZOOM_VISUAL_PRESETS`：更新各层 sigmaScale（region:1.55, area:1.18, neighborhood:0.95, street:0.72）、contrastScale、boundaryScale；新增 densityBias 字段；移除未使用的 mapBrightnessScale/mapContrastScale/mapOpacityScale
- 新增 `ZOOM_ANCHORS`（4 个锚点：zoom 11/13/14.3/16.2）
- 新增 `interpolateZoomPreset()`：连续插值 numeric 字段，boolean 字段 t≥0.5 切换
- 新增 `getContinuousZoomPreset()`：用 smoothstep 在 ZOOM_ANCHORS 之间插值，覆盖全部 zoom 范围
- 移除旧 `interpolatePreset()` 和 `getZoomPreset()`
- `gridRenderConfig()`：改用 `getContinuousZoomPreset(zoom)`；按屏宽区分 maxDotsDesktop/maxDotsMobile
- `applyMapVisualMode()`：改用 `getContinuousZoomPreset()`
- `buildGeoGridField()`：spacing 超限调整改为 `Math.pow(ratio, 0.35)` clamped 1.28（原为 sqrt）；base 0.018→0.010
- `drawGridTexture()`：引入 `baseSkinAlpha = 0.010` 对所有点（active 和 void）保证最低可见度；void 点 rawAlpha = baseSkinAlpha + getDotAlpha * 0.22

### Files Modified

- `test0.2.html`
- `MAP_LOG`

### Known Limitations

- ZOOM_ANCHORS 选取（11/13/14.3/16.2）基于估算，真机测试后可能需微调
- densityBias 字段当前已插值但 gridRenderConfig 未使用，留作后续优化接口

## Revert Structure Lines / 2026-05-12

### Problems

- 地图上出现明显线条（corridor / rail polyline），破坏 World Skin 视觉
- zoom 明暗变化过于剧烈（mapBrightnessScale 跨度 0.88–1.16）
- 地图可读性修复方向偏成线框叠加图

### Changes

- 移除 `mapStructureOverlay` canvas 元素及 CSS
- 移除 `STRUCTURE_LINE_STYLE`、`structureCanvas`、`structureCtx`
- 移除 `drawStructureOverlay()` 和 `requestStructureRender()`
- 还原 `#skinCanvas` z-index 为 2
- 重写 `getMapVisualByHour()`：白天 brightness 上限 0.78，夜晚 0.50，overlayDarkness 白天 0.26、夜晚 0.48，不再引用 MAP_TIME_VISUAL 常量
- 新增 `getZoomLightAmount()` 和 `getZoomVisualModifier()`：zoom 只做微调（brightnessMul 0.92–1.06，opacityMul 0.94–1.04，overlayAdd ±0.05）
- 重写 `applyMapVisualMode()`：先计算 timeVisual，再叠加 zoomMod，zoom 不再覆盖时间明暗
- `applyMapVisualMode()` 不再引用 `preset.mapBrightnessScale / mapOpacityScale`

### Files Modified

- `test0.2.html`
- `MAP_LOG`

### Known Limitations

- 地图可读性现在完全依赖 MapLibre raster tile 自身纹理 + CSS filter；无手绘结构线
- 如纹理仍不够可读，可小幅调高 `rasterContrastBase` 或 `edgeBoost`，不要再加线层

### Next Task

- 浏览器检查 05:00、13:00、21:00 底图明暗
- 检查 zoom in / out 时明暗变化是否足够克制（不超过视觉感知 15%）

## Map Readability Rework / 2026-05-12

### Problems

- 地图整体过亮，浅灰底削弱了点阵皮肤的主视觉
- 地图亮但信息不可读：道路、水系、铁路、街区边界缺少层级，呈现为一片灰色纹理
- 单纯提高 raster 亮度无法改善结构可读性

### Changes

- `MAP_TIME_VISUAL` 全时段亮度下调：day.brightness 从 1.05 → 0.76，overlayDarkness 从 0.10 → 0.24，deepNight.brightness 从 0.52 → 0.44，deepNight.overlayDarkness 从 0.52 → 0.60
- 新增 `#mapStructureOverlay`（canvas，z-index 2，`mix-blend-mode: screen`），位于 mapDarkOverlay 与 skinCanvas 之间
- 新增 `STRUCTURE_LINE_STYLE` 常量，定义 rail / majorRoad / minorRoad / station 四类结构线参数
- 新增 `drawStructureOverlay()` 函数：使用 `WORLD_SKIN_DATA.corridors` 绘制铁路与道路结构线，使用 `WORLD_SKIN_DATA.clusters` 在 neighborhood/street 层绘制车站环；线条极淡（alpha 0.12–0.30），随 zoom 层级淡化
- 新增 `requestStructureRender()` 按需帧调度
- 在地图 move/zoom/rotate/pitch 事件、load 事件、switchView 时触发结构线重绘
- `resizeCanvas()` 同步调整结构线 canvas 尺寸与 DPR 变换
- `#skinCanvas` z-index 从 2 提升至 3

### Files Modified

- `test0.2.html`
- `MAP_LOG`

### Known Limitations

- 当前使用 raster tile（voyager_nolabels），无法精确提取矢量道路；结构线基于模拟 corridor 数据，不是真实路网
- 结构线只覆盖已定义的 corridors 和 clusters，稀疏地区没有结构线辅助
- `mix-blend-mode: screen` 在点阵皮肤上方可能有轻微颜色交互，需要真实设备截图验证

### Next Task

- 在浏览器中检查 05:00、13:00、21:00 三个时间点的底图明暗，确认白天最大亮度已压低
- 检查结构线在 area / neighborhood 层级是否可读但不抢点阵
- 如觉得结构线仍不够明显，可提高 STRUCTURE_LINE_STYLE 各项 alphaBase（不超过 0.45）

## Stable Dot Density / 2026-05-12

### Problems

- 不同缩放层级下点阵密度变化过大。
- 缩放时点阵会因为 `dotSpacingMeters` preset 突然过密或过稀。
- 当前需要保持截图中的点阵视觉密度和规则皮肤感。

### Changes

- 新增 `DOT_DENSITY_VISUAL`，以 `targetScreenSpacingPx: 15` 作为点阵视觉密度目标。
- 将点阵密度控制改为 `screen-stable`：通过 MapLibre `project / unproject` 从地图中心反算当前 15px 约等于多少米。
- `ZOOM_VISUAL_PRESETS` 不再直接控制最终点阵间距或点半径，只继续控制 sigma、alpha、height、contrast、boundary 和底图可读性。
- `buildGeoGridField()` 使用稳定屏幕点距生成地理网格，并通过 `maxDots / minDots` 自动调节实际 spacing，避免极端缩放下过密或过稀。
- `getDotRadius()` 改为使用 `DOT_DENSITY_VISUAL` 的半径范围，半径主要由数据强度、flux、boundary 和 word bias 控制，不再随 zoom preset 大幅变化。
- Debug panel 增加目标屏幕点距显示，方便检查缩放时密度是否稳定。

### Files Modified

- `test0.2.html`
- `MAP_LOG`

### Known Limitations

- pitch 下不同屏幕 y 的米/像素差异目前先用地图中心点近似。
- 当前仍使用 Canvas 2D 贴地投影，后续如果需要更精确透视，可按屏幕深度分段修正 spacing。

### Next Task

- 在浏览器中测试 `region / area / neighborhood / street` 四档 zoom，确认点阵屏幕间距保持在约 12–18px，并检查移动端性能。

## Time-Based Map Brightness / 2026-05-12

### Problems

- 背景地图底层过亮。
- 时间轴未影响地图昼夜明暗。
- 白天最大亮度仍压不住点阵主视觉。

### Changes

- 新增 `MAP_TIME_VISUAL`，定义 day / dawn / dusk / night / deepNight 的底图亮度、对比、饱和、opacity 和 overlay darkness。
- 新增 `getDaylightState()`、`getDaylightAmount()`、`getMapVisualByHour()`，使用东京近似日出日落时间计算连续昼夜明暗。
- 新增 `#mapDarkOverlay`，位于 MapLibre 底图之上、点阵 Canvas 之下，只负责压暗底图，不影响点阵和 UI。
- `applyMapVisualMode()` 现在叠加 zoom preset 与 time visual：zoom 控制信息密度，hour 控制整体明暗。
- 时间轴拖动时同步更新 MapLibre filter、raster opacity 和暗化 overlay。
- 夜间点阵 alpha 增加最多约 10%，让夜间底图压暗后沉积仍可读，但不做霓虹化。
- Debug panel 增加昼光值和时间状态，默认仍关闭。

### Files Modified

- `test0.2.html`
- `MAP_LOG`

### Known Limitations

- 当前日出日落使用东京固定近似值，未接入真实季节 / 天气 API。
- raster 底图只能整体调节 filter / opacity，无法按道路、水系、铁路分别精细控制。

### Next Task

- 在浏览器中检查 02:00、05:00、15:00、18:00、21:00 五个时间点的底图明暗，微调 `MAP_TIME_VISUAL` 的 brightness 和 overlayDarkness。

## Zoom Information Layering / 2026-05-12

### Problems

- 不同缩放层级显示的信息量几乎相同。
- 缩远时点阵和底图细节过多，整体城市皮肤形态不够清楚。
- 缩近时局部记录、街区边界和材料差异不够明显。

### Changes

- 新增四档 World Skin zoom level：`region / area / neighborhood / street`。
- 新增 `ZOOM_VISUAL_PRESETS`，统一控制点阵间距、半径、alpha、高度、sigma、contrast、boundary、底图强度和最大点数。
- `World Map` 默认 `zoom: 13.1`，落在 `area` 层级，作为作品集默认展示构图。
- `My Radius` 默认 `zoom: 14.1`，落在 `neighborhood` 层级，更适合个人生活圈查看。
- `gridRenderConfig()` 改为从当前 `map.getZoom()` 读取 preset，并按层级调整 `dotSpacingMeters / sigmaMeters / maxInfluenceDistance / cellSize`。
- 数据场计算中加入 `contrastScale / boundaryScale / heightScale`，让缩远更平滑、缩近更局部。
- 绘制时加入 `alphaScale / dotRadiusScale`，让不同 zoom 下点阵密度和视觉重量更稳定。
- 根据 viewport 和 preset `maxDots` 自动提高实际 spacing，避免 street 层级点数过多。
- 地图底层 filter 和 raster contrast 随 zoom preset 调整：缩远更克制，缩近增加道路/街区骨架可读性。
- Debug panel 增加当前 zoom 階層显示，默认仍关闭。

### Files Modified

- `test0.2.html`
- `MAP_LOG`

### Known Limitations

- 当前底图是 raster no-label tiles，无法像 vector style 一样分别控制主路、小路、铁路、水系和地块边界，只能通过整体 filter / raster contrast 分层。
- 缩放层级边界附近只对数值参数做轻量插值，布尔开关仍按当前层级切换。
- 真实移动端 street 层级仍需手动检查点数和帧率。

### Next Task

- 用浏览器逐级测试 zoom：`region` 看整体皮肤、`area` 看默认生活圈、`neighborhood` 看街区边界、`street` 看局部 imprint。
- 如果 street 层级移动端仍偏重，优先把 `street.dotSpacingMeters` 从 `32` 提高到 `40`。

## Map Base Visibility Fix / 2026-05-12

### Problems

- 底层地图仍然几乎不可见。
- 画面存在方向性明暗差：点阵 / 地图在左上更明显，右下更淡。

### Changes

- 将底图瓦片从 `dark_nolabels` 切换为信息量更高的 `voyager_nolabels`，继续保持无文字、无 POI、无 marker。
- 用 CSS filter 将 Voyager 无文字底图压暗为深色城市骨架，而不是继续使用几乎纯黑的 dark raster。
- 降低 `.shade` 和地图外框 inset shadow 的压黑强度，移除左右方向上的固定暗化渐变。
- 压缩点阵深度衰减范围：从强烈的上亮下暗改为轻微透视变化，避免拖动方向造成“左上清楚、右下变淡”的错觉。
- 重新降低 raster paint contrast，避免和 CSS contrast 叠加后破坏底图层次。

### Files Modified

- `test0.2.html`
- `MAP_LOG`

### Known Limitations

- 底图仍依赖 Carto raster tiles 外部网络。
- 这次通过静态检查和瓦片 HEAD 请求验证资源可达，仍需要浏览器截图确认最终明暗。

### Next Task

- 在浏览器中分别把地图拖到左上、右下、中心，确认底图可读性是否一致；如果仍偏暗，只微调 `MAP_VISUAL_MODES` 的 brightness / contrast。

## Map Interaction Fix / 2026-05-12

### Problems

- 地图无法拖动 / 缩放。
- 背景地图纹理不可读，点阵像漂在黑色背景上。
- 初始视图不稳定，进入 World Map / My Radius 时可能过近、过暗或构图不舒服。

### Changes

- 修复 pointer-events 层级：`#skinCanvas` 默认不拦截 MapLibre，只有 MapLibre 未就绪的 fallback 状态才允许 Canvas 接管。
- 为 `#realMap` 设置允许地图手势的 touch 行为，避免全屏视觉层阻断地图。
- 恢复并显式启用 MapLibre `dragPan / scrollZoom / doubleClickZoom / touchZoomRotate / dragRotate / keyboard`。
- 将原先空格拼接的地图事件绑定拆成逐个事件绑定：`move / zoom / rotate / pitch` 触发轻量重绘，`moveend / zoomend / rotateend / pitchend` 再延迟重建 skin field。
- 拖动和缩放过程中，缓存点阵根据 `mx / my -> lng / lat -> map.project([lng, lat])` 重新投影，避免点阵固定在屏幕上。
- 统一 `DEFAULT_VIEW`：国分寺中心、`zoom: 13.1`、`pitch: 42`、`bearing: -16`。
- World Map 切换时回到稳定默认视角；My Radius 切换时使用当前个人半径中心和稍近的 `zoom: 14.1`。
- 增加 `R` 键 reset view 到当前视图的默认构图。
- 提高 MapLibre raster 底图亮度、对比和 opacity，并降低 Canvas / shade 对底图的压黑。
- 地图仍使用 no-label raster tiles，不显示文字、POI、店铺、marker 或导航 UI。
- Debug panel 增加 zoom / pitch / bearing / map moving / Canvas pointer-events 状态，用于检查交互层级。

### Files Modified

- `test0.2.html`
- `MAP_LOG`

### Known Limitations

- 本次完成了静态检查、脚本语法检查和本地 HTTP 资源检查，但没有真实浏览器 DevTools 自动化拖拽测试。
- MapLibre 和 raster tiles 仍依赖外部网络。
- `test0.2.html` 仍是单文件原型，交互层级后续拆分时需要再次回归检查。

### Next Task

- 用真实浏览器和手机进行手动验收：单指拖动、滚轮缩放、双指缩放、旋转、时间轴拖动、World / My Radius 视图切换。
- 若仍觉得底图过暗，下一步只调整 `MAP_VISUAL_MODES` 和 `.shade` 透明度，不改点阵数据系统。

## Integrity Check / 2026-05-12

### Entry file

- 当前入口文件为 `test0.2.html`。
- 当前目录没有 `index.html`。
- `test0.1.html` 仍存在，但作为旧版备份；`test0.2.html` 没有引用 `test0.1.html`。

### Files inspected

- `test0.2.html`
- `world_data.js`
- `MAP_SPEC`
- `TODO`
- `MAP_LOG`
- `BUGS`
- 当前未发现 `manifest.json`。
- 当前未发现 `service-worker.js`。
- 本地静态资源目录 `作品集准备（codex勿读）/` 未被入口文件引用。

### Problems found

- World Map 渲染管线主要读取个人记录格式字段：`noise / flux / movement / createdAt`。虽然 `world_data.js` 已保留兼容字段，但渲染层缺少显式兼容保护，后续数据文件变动时容易产生 `NaN` 场值。
- 部分用户可见 UI / toast / debug 文案仍混有英文或中文，不符合当前“用户可见文案使用日文”的要求。
- `localStorage` 写入没有错误保护，隐私模式或存储不可用时可能中断记录流程。
- 当前没有 `manifest.json` / `service-worker.js`，因此还不是完整 PWA 包装。
- 未发现 `?demo=1` 分支。
- 本次检查通过静态语法、数据验证、HTTP 资源检查完成；未在真实浏览器 DevTools 中执行完整 console 交互检查。

### Bugs fixed

- 新增 `recordNoise()`、`recordFlux()`、`recordMove()`、`recordTime()`、`recordSlot()` 兼容层，让 World 聚合数据和 My Radius 个人数据都能进入同一套 visual field 计算。
- `temporalWeight()` 改为兼容 `timestamp / createdAt`，并优先使用记录自带 `hour`。
- `buildGeoGridField()` 对参与计算的记录写入标准化 `noise / flux / movement / slot / createdAt`，避免 World Map 数据字段差异造成 `NaN`。
- 将主要用户可见静态文案、toast、debug panel 文案调整为日文。
- 为 `saveRecords()` 增加 `try/catch`，`localStorage` 写入失败时显示日文提示且不中断应用。
- `world_data.js` 缺失时增加 console warning，并回退为空聚合记录，不阻断启动。
- Sense 采样完成后的记录确认面板现在会显示本次音量、揺らぎ和移动状态，不再停留在初始 0.00 / 静止。

### Visual state

- 当前视觉方向保持：深色 MapLibre 底图、45 度 pitch、-16 bearing、无文字/POI/marker。
- Canvas 点阵为地理米制规则点阵，通过 `map.project([lng, lat])` 投影到 MapLibre 当前视图。
- World Map 使用 `world_data.js` 聚合数据；My Radius 使用 `localStorage` 个人记录。
- 点阵支持沉积核心、空洞、通勤走廊、住宅底层纹理、冷灰/银白/锈色偏置和静态 height 场。
- Canvas 没有不透明黑色背景；地图上方只有低 alpha 暗化和点阵皮肤。

### Performance state

- 代码中未发现 `getDotJitter`、`pseudoNoise`、`getPulse`。
- 空闲状态没有无条件常驻重绘；`requestAnimationFrame` 由 `requestRender()`、Sense 采样和 field transition 触发。
- 数据场使用地理网格 + spatial hash；网格点只查询附近 cell，不再每帧完整遍历 `dots × records`。
- 未发现逐点 `shadowBlur` 或逐点 radial gradient glow。
- 本地 HTTP 检查通过：`test0.2.html` 与 `world_data.js` 均返回 200。

### Mobile state

- HTML 含 viewport meta。
- CSS 使用 `100vh / 100dvh`、`safe-area-inset-*`、底部控件留出 home indicator 空间。
- MapLibre 启用 `dragPan / scrollZoom / touchZoomRotate / dragRotate / doubleClickZoom`。
- 地图就绪时 Canvas 设置为 `pointer-events: none`，避免阻断 MapLibre 手势。
- Sense 长按使用 pointer events，采样期间禁用选择和默认触摸滚动。
- 未在真实手机设备上完成手动验证，仍需补测权限弹窗和触摸取消。

### Known limitations

- 当前没有正式 `index.html`。
- 当前没有 PWA manifest / service worker / icon 配置。
- MapLibre JS/CSS 和 raster tiles 依赖外部网络。
- 没有真实浏览器 DevTools console 自动化测试；本次只完成脚本语法、数据结构、资源可达性和静态管线检查。
- `?demo=1` 当前未实现。
- `test0.2.html` 仍是单文件原型，后续维护成本偏高。

### Next recommended task

- 先用真实手机浏览器打开 `test0.2.html` 做一次手动交互验收：World Map 拖拽/缩放/旋转、时间轴、Sense 10 秒采样、My Radius 写入和刷新恢复。
- 之后决定作品集入口是否改为 `index.html`，再进入 PWA 包装或文件拆分。

## 2026-05-12

### 稳定性能版：3D 斜视地理点阵与静态高度场

用户反馈：

- World Skin 网页明显变卡。
- 怀疑卡顿来自点阵抖动、逐点动画、逐帧重复计算、glow 绘制过多、每帧遍历所有 records × grid nodes。
- 今晚目标是稳定版本：略带俯视角的 3D 斜视图，点阵贴地，数据形成静态起伏高度场。

本次调整：

- 关闭并移除绘制路径中的 `pseudoNoise`、`getDotJitter()`、逐点随机偏移、逐点 pulse、逐点 radialGradient glow。
- 渲染循环改为按需绘制，不再无条件 `requestAnimationFrame(render)` 常驻重绘。
- 只有地图变化、时间轴变化、数据源切换、新记录、resize、Sense 采样或时间场插值时才绘制。
- MapLibre 默认视角改为 `zoom: 13.2`、`pitch: 45`、`bearing: -16`、`maxPitch: 60`。
- 点阵从 screen-space 网格改为地理米制网格，再通过 `map.project([lng, lat])` 投影到屏幕，使点阵跟随 pitch / bearing。
- 数据场计算改为局部米制坐标 + spatial hash，网格点只查询附近 cells 内记录。
- 移除逐点 glow，保留 alpha / radius / color / static height 表达差异。
- 新增静态 height 场：密集处轻微上移、变亮、变厚；空洞处低矮、暗、薄。
- 时间轴拖动时计算 target field，并用约 420ms 插值，不拖动时不自动播放。
- `MAP_SPEC` 与 `TODO` 已同步。

验证：

- `test0.2.html` 内联脚本语法通过。
- `world_data.js` 语法通过。
- 代码中已无 `getDotJitter`、`pseudoNoise`、`getPulse` 调用。

## 2026-05-11

### 点阵激活视觉与地图骨架可读性

用户反馈：

- 点阵仍然过平、过暗、过均匀。
- 主观词和数据场差异不够明显。
- 地图底层过黑，点阵像漂在黑底上，不像附着在真实城市表面。

本次调整：

- 新增 `GRID_VISUAL`，统一控制 spacing、radius、alpha、glow、jitter、pulse、influence cutoff 和色板。
- 点阵仍然使用规则屏幕网格，但 active 区域允许受限半径变化、轻微 glow、微抖动、缓慢 pulse 和少量锈色。
- `WORD_VISUAL_BIAS` 扩展 `radiusBias / glowBias / jitterBias / rustBias`，让 8 个主观词的触感更明显。
- 新增 `getDotRadius()`、`getDotGlow()`、`getDotJitter()`、`getPulse()`。
- 绘制顺序改为：弱 void 点阵、active glow、核心点。
- 新增 `MAP_VISUAL_MODES`，默认 balanced；`M` 键可在 dark / balanced / bright 三档间切换。
- 地图底层亮度、对比、饱和度、opacity 和 raster contrast 重新调整，继续隐藏文字、POI、marker。
- `.shade` 遮罩减弱，避免压黑地图道路、水系、铁路和街区边缘。
- `world_data.js` 中 station / corridor 权重提高，station 采样半径收紧，corridor 横向偏移收窄。
- 当前模拟数据生成 2697 条：station 892、corridor 683、school 356、park 304、residential 361、isolated 101。
- `MAP_SPEC` 与 `TODO` 已同步。

验证：

- `test0.2.html` 内联脚本语法通过。
- `world_data.js` 语法通过。

## 2026-05-11

### 提高 World Skin 点阵可读性

用户反馈：

- 点阵画面过于平均、模糊、低对比。
- 数据变化、沉积、空洞和边界差异几乎不可见。

本次调整：

- 将点阵 alpha 上限从 `0.48` 提高到 `0.58`。
- `strength < 0.025` 的节点只绘制极暗空洞点，不再叠加 active base。
- active 点改用非线性 alpha：strength / contrast / boundary 分别按幂函数拉开动态范围。
- 缩小并按 zoom 调整 sigma：area `128px`、mid `88px`、street `62px`。
- 加入 `influenceCutoff = 0.012`，远距离微弱影响不再参与累加。
- 提高相邻节点差异对 `contrast` 和 alpha 的影响，让边界带更容易出现断续裂感。
- 更新点阵色板：void 深冷灰、cold/neutral/warm/rust 低饱和插值。
- 地图底层亮度、对比和 opacity 提高，继续隐藏地名、POI 和 marker。
- 加入 `D` 键 Debug View，显示 records、screen records、strength、contrast、alpha、sigma、spacing 和 zoom。
- `MAP_SPEC` 与 `TODO` 已同步。

## 2026-05-11

### 修复 World Skin 点阵不可见

用户反馈：

- 当前打开后完全看不到地图纹理。

本次调整：

- 修正显示层级：`#skinCanvas` 提升到 `.shade` 之上，避免点阵被遮罩压暗。
- `.view` 和 UI 层保持在点阵之上，避免 Sense / Radius 控件被 Canvas 盖住。
- `#app` 增加 `height: 100vh` fallback，保留 `100dvh`。
- `resizeCanvas()` 在 `getBoundingClientRect()` 读到 0 尺寸时回退到 `window.innerWidth / window.innerHeight`。
- `BUGS` 新增 005，用于后续检查点阵不可见问题。

## 2026-05-11

### 基于真实地图结构重写 World Map 预设数据

用户要求：

- 覆盖当前 `world_data.js`。
- 生成一组国分寺周边约 5km、100 用户、连续 7 天的模拟公共聚合使用数据。
- 数据不能在圆形范围内随机撒点，必须依附真实地图结构。
- 车站、道路/铁路走廊、学校、公园、住宅区、空洞和孤岛记录要有不同分布与声音/感受特征。

本次调整：

- 重写 `world_data.js` 为 `kokubunji-structure-sim-v2`。
- 默认中心改为 `{ lat: 35.7000, lng: 139.4800 }`。
- 生成 2626 条模拟记录，字段包含 `id / userId / lat / lng / timestamp / hour / weekday / noiseLevel / turbulence / peak / mobility / direction / duration / word / trustScore`。
- 保留与当前原型兼容的字段：`noise / flux / movement / distance / slot / createdAt / source`。
- 新增结构数据：`WORLD_SKIN_ZONES`、`WORLD_SKIN_CORRIDORS`、`WORLD_SKIN_VOIDS`。
- 车站节点包括国分寺駅、西国分寺駅、恋ヶ窪駅、一橋学園駅。
- 走廊包括中央線、西武国分寺線、府中街道方向、国分寺南北生活动线。
- 学校/公共设施包括武蔵野美術大学、東京経済大学周辺。
- 公园/绿地包括都立武蔵国分寺公園、殿ヶ谷戸庭園、野川・小金井方面绿地边缘。
- 住宅肌理与孤岛记录用于生成低密度生活皮肤和偶发痕迹。
- 空洞区域通过排除区保留，用于测试 Void 层。
- `MAP_SPEC` 与 `TODO` 已同步。

验证：

- `node -c world_data.js` 通过。
- 当前记录数：2626。
- 类型分布：station 723、corridor 567、school 353、park 350、residential 494、isolated 139。

## 2026-05-11

### 主观感知词 wordBias 映射

用户要求：

- 优化 8 个主观感知词对点阵皮肤的影响。
- 主观词不直接显示，不生成标签、图标、emoji、marker 或额外图形。
- 只能影响 alpha、色温、contrast、flux/turbulence 解释、局部边界感和沉积厚度感。

本次调整：

- `wordBias()` 已从旧的 `lift / jitter / glow / warm` 改为稳定参数表。
- 词语参数表现在是 `WORD_VISUAL_BIAS` 常量。
- 新增 `recordBaseVisual(record)`，先从声音平均值、波动性、移动状态、时间段推导基础视觉参数。
- 新增 `applyWordBias(base, word)`，按用户指定规则将主观词偏置叠加到基础参数。
- 新增 `getDotAlpha(params)` 与 `getDotColor(params)`，统一点阵透明度和色温计算。
- 新参数包括 `alphaBias / contrastBias / fluxBias / warmthBias / spreadBias / boundaryBias / densityBias / breathSpeedBias`。
- 8 个词均按用户给定建议建立映射：`ざらつく / 重い / 浮く / 乾く / 遠い / 詰まる / ほどける / 眠い`。
- 高斯场计算会累加 wordBias，并 clamp 到安全范围。
- 点的位置、半径、形状不受 wordBias 影响。
- `MAP_SPEC` 与 `TODO` 已同步。

## 2026-05-11

### 重写稳定二维点阵覆盖层

用户要求：

- World Skin 覆盖层目标不是粒子动画、热力图、星空、发光云团或随机噪声。
- 在真实地图上覆盖一层稳定、规则、半透明的点阵皮肤。
- 点阵由固定屏幕 spacing 生成，点半径固定。
- 记录必须用 `map.project([lng, lat])` 转换为屏幕场源，再影响周围网格节点。
- 数据只能改变 alpha、色温、局部对比，不改变点的位置、大小、形状。

本次调整：

- `drawGridTexture()` 改为每帧生成固定屏幕二维网格。
- 每条记录在当前 MapLibre 视图中用 `map.project([lng, lat])` 得到屏幕坐标。
- 用屏幕距离和高斯衰减计算每个网格节点的 `strength / flux / noise / wordBias`。
- 相邻节点差异计算 `contrast`，用于形成细微边界感。
- 移除上一版数据柔光云团/热力渐变，只绘制固定半径点阵。
- 点阵半径固定为 `VISUAL_LIMITS.radius`，数据只影响 alpha 和低饱和色温。
- 为避免爆亮，alpha、strength、flux、contrast 继续使用阈值 clamp。
- `MAP_SPEC`、`TODO` 已同步。

## 2026-05-11

### 地图层级、米制点阵与当前位置方向

用户要求：

- 地图黑色遮罩再减弱一点。
- 中心需要标识当前所在位置和方向。
- 加入数据视觉效果，参考点阵属性映射图。
- 修复地图缩放时点阵不跟随缩放的问题。
- 参考 Google Maps 模式，建立几个地图层级，到一定缩放程度自动切换。

本次调整：

- 航拍底图遮罩进一步减弱，底图亮度略提高。
- 新增当前位置/方向标记：显示中心点、呼吸 halo 和方向箭头。
- 点阵从屏幕固定网格改为真实地图米制网格，节点随 MapLibre 缩放、旋转、平移重新投影。
- 新增 area / mid / street 三个自动层级：
  - area 使用较大米制间距。
  - mid 使用默认米制间距。
  - street 使用更细米制间距。
- 数据视觉进一步接近参考图：strength 控制点 alpha，flux/contrast 提升柔光，time 影响冷暖倾向，wordBias 影响色温。
- `MAP_SPEC` 与 `TODO` 已同步。

## 2026-05-11

### 按总参考图统一视觉系统

用户要求：

- 根据整体视觉参考图完整修正视觉效果。

本次调整：

- 主画面改为更接近参考图的暗色全屏地图构图。
- 桌面布局改为左侧竖向导航、地图外框、底部 24H 时间轴、右上地图控制。
- 移动端保留底部导航，避免窄屏被左侧导航挤压。
- 点阵仍保持固定半径，但新增数据驱动的冷暖柔光场，用于表达区域倾向。
- 地图页继续使用航拍底图，Canvas 只负责暗角、点阵和数据光场。
- My Radius 信息抽屉在桌面端改为右侧抽屉，更接近参考图中的信息 drawer。
- Sense 页面改为更宽的分区构图，采样圆环和日语提示保留。
- `MAP_SPEC` 与 `TODO` 已同步。

## 2026-05-11

### 航拍底图、移除椭圆膜与音波 bug 修复

用户要求：

- 将地图回退到正航拍图。
- 中间不应该是椭圆。
- 当前完全看不到地图，需要提高地图可见度。
- 修复严重 bug：不管在哪个页面按都会出现音波。
- 新建一个 bug 文档，方便后续检查。

本次调整：

- `test0.2.html` 的 MapLibre 底图改为 Esri World Imagery 航拍 raster tile。
- 地图页关闭中心椭圆膜和同心椭圆，只保留轻微暗角，避免遮住航拍图。
- 降低 Canvas 地图页遮罩强度，提高航拍底图可见度。
- Sense 采样只允许在 `state.view === "sense"` 时开始。
- 音波绘制只允许在 Sense 页面且正在录制时出现。
- 离开 Sense 页面会自动 `cancelRecording()`。
- 非 active view 使用 `visibility: hidden` 防止隐藏页面误触，同时保持 view 容器不拦截地图操作。
- 新增 `BUGS` 文档，记录 bug 001 和底图可见度回归检查项。

## 2026-05-11

### 迁移 MapLibre 并回退水平点阵

用户要求：

- 加入 Google Maps 同款地图操作方法，包括转向、滚动、放大缩小。
- 做几个层级的地图显示。
- 将一整层点阵纹理回退到水平，方便下一步修改。
- 附近边缘太少，需要更多，并且要跟真实地图一致。

本次调整：

- `test0.2.html` 从 Leaflet 迁移到 MapLibre GL。
- 地图支持拖拽、滚轮缩放、双指缩放、双指旋转、右键/触控板旋转和键盘导航。
- 新增 area / mid / street 三个地图显示层级，根据缩放调整无文字瓦片的亮度、对比度和透明度。
- 点阵纹理回退为水平二维覆盖层，不再使用 pseudo-3D 高度投影。
- 移除手绘地图边界渲染，改为增强真实无文字地图瓦片中的道路、地块、水系边缘，避免边界和真实地图不一致。
- `MAP_SPEC` 与 `TODO` 已同步更新。

## 2026-05-11

### 接入真实地图引擎

用户要求：

- 需要和真实的地图引擎联动。

本次调整：

- `test0.2.html` 接入 Leaflet 作为真实地图引擎。
- 底层使用无文字地图瓦片，避免显示地名、POI 和普通导航地图信息。
- Canvas 点阵与边界层改为通过 Leaflet 的经纬度投影结果对齐。
- World Map / My Radius 的拖拽、缩放和触摸缩放优先交给 Leaflet 处理。
- 地图引擎加载失败时保留原本的本地 Canvas 投影作为 fallback。
- `MAP_SPEC` 与 `TODO` 已记录真实地图引擎联动方案。

## 2026-05-11

### 回退点阵动态并隐藏地图文字

用户要求：

- 优化显示效果，删除文字跟随摆动。
- 只保留点阵，将所有和点有关的放大缩小回退删除。
- 地图文字不显示，但边界需要可见，中心边界最强，越到周边越淡。

本次调整：

- `test0.2.html` 已移除地图地名绘制，不再调用文字标签层。
- 点阵改为固定半径，不再做膨胀、收缩、快速抖动、外发光或点间连线。
- 数据仍影响点阵透明度、色温和 pseudo-3D 高度，但视觉变化更稳。
- 地图边界改为径向透明度衰减：中心区域更清楚，周边逐渐淡出。
- `MAP_SPEC` 与 `TODO` 已同步更新为新的显示规则。

## 2026-05-11

### 加入显示阈值

用户要求：

- 加入显示阈值，无论如何都不会超过这个阈值显示。

本次调整：

- 在 `test0.2.html` 中新增 `VISUAL_LIMITS`。
- 对 `strength / flux / contrast / height / wave / jitter / radius / alpha / glow / lineAlpha` 加入硬限制。
- 不管数据如何叠加，节点高度、发光、透明度、半径、抖动和网格线透明度都不会超过设定上限。
- `MAP_SPEC` 已记录当前阈值和后续新增视觉变量的规则。

## 2026-05-11

### 外部数据文件与 24 小时时间轴

用户要求：

- 完全删除当前嵌入的预设数据。
- 新建文件储存所有数据。
- 加入数据到视觉的控制算法，可按月、周、三日、日、小时等窗口综合叠加。
- 加入时间控制系统，在大地图上显示时间轴，参考 Apple Weather，可滑动查看 24 小时变化。
- 在新数据文件中加入国分寺周边 5km、100 个用户、一周使用结果。
- 粒子抖动仍太快，需要降低。

本次调整：

- 新增 `world_data.js`，主文件不再内嵌预设数据生成器。
- `world_data.js` 以国分寺为中心，生成 100 用户一周模拟记录。
- 模拟数据基于国分寺駅、武蔵国分寺公園、一橋大学、西国分寺、小金井公園方面、南町住宅地、府中街道、商業エリア等使用场景。
- `test0.2.html` 通过 `<script src="world_data.js"></script>` 加载数据。
- World Map 新增 24 小时时间轴 slider。
- 时间滑动会重建视觉场。
- 视觉算法新增多窗口权重：hour 0.44、day 0.24、threeDay 0.16、week 0.11、month 0.05。
- 网格运动频率大幅降低，从快速 jitter 调整为接近天气图层的缓慢变化。
- `MAP_SPEC` / `TODO` 已同步更新。

## 2026-05-11

### 修正地图和点的 3D 同步与可读性

用户反馈：

- 后面的地图没有 3D。
- 地图和点需要完美同步。
- 点太密集、太杂乱，后面的文字看不清。

本次调整：

- 地图边界、地名和网格点现在共用同一套高度场投影。
- 地图线条先绘制在网格下方，地名和关键边界再绘制在网格上方。
- 地名增加暗色描边和 halo，避免被发光点遮住。
- 网格密度降低：World 点间距从 22 调整到 34，My Radius 从 24 调整到 36。
- 进一步加强 z-height 和透视压缩，让波峰/波谷更明显。
- 降低网格线 alpha、点 alpha 和 glow，减少杂乱感。
- `MAP_SPEC` 已同步写入地图/点同投影与可读性规则。

## 2026-05-11

### 加强纹理堆叠并加入地图导航

用户要求：

- 纹理堆叠效果需要根据附近环境变化和用户感受选择更明显地体现。
- 大地图需要支持拖拽和放大缩小。

本次调整：

- 每个网格节点新增 `wordBias` 影响：高度、抖动、发光、色温。
- 每个节点计算相邻节点强度与波动差，得到 `contrast`。
- `contrast` 会提高节点高度、发光、半径和网格线透明度，让局部环境变化形成更明显的脊线和波峰/波谷。
- World Map 和 My Radius 支持拖拽平移。
- 支持鼠标滚轮缩放和触摸双指缩放。
- 中心按钮现在会同时重置 pan 和 zoom。
- `MAP_SPEC` 已同步更新纹理堆叠与地图交互规则。

## 2026-05-11

### 地图改为 pseudo-3D 并重构 My Radius 信息层

用户要求：

- 地图需要改成 3D，可以看出波峰波谷。
- My Radius 的信息显示太差，点进去应该先是地图，再通过按钮下拉显示具体信息。

本次调整：

- Canvas 网格渲染加入 pseudo-3D 投影。
- 每个网格节点根据 `strength` 和 `wave` 计算高度 `z`。
- 节点屏幕位置由 `(x, y, z)` 经过简化透视投影得到，高强度区域向上隆起，低强度区域下沉。
- 相邻网格节点之间新增细线连接，让波峰/波谷形成可读地形。
- My Radius 视图默认只显示地图。
- My Radius 的统计、时间剖面和词分布移入下拉详情抽屉，通过 `details ↓` / `map ↑` 切换。
- `MAP_SPEC` 已同步更新 3D 网格与 My Radius 抽屉规则。

## 2026-05-11

### 重做 Sense 呼吸按钮

用户反馈：

- 按钮界面的呼吸效果太弱，看不出呼吸。
- 希望改成几个白色线条同心圆，按呼吸节奏放大缩小。
- 文字提示太少，需要至少有类似“静下心来，感受一下周围的世界”的日语提示。

本次调整：

- Sense 按钮从有机 blob 改成 4 层白色线条同心圆。
- 同心圆共享 `--breath-scale`，按 5 秒吸气 + 5 秒呼气明显放大缩小。
- 增加日语初始提示：`静かにして、まわりの世界を感じる`。
- 采样时按呼吸阶段切换提示：`息を吸って、世界を近くに置く` / `息を吐いて、音をほどく`。
- `MAP_SPEC` 已同步更新采样呼吸引导规则。

## 2026-05-11

### 拆分 World Map / My Radius 并改为网格纹理（旧方案已废弃）

用户要求：

- 将“我的半径”和“世界地图”切分为两个功能。
- 加入一组预设聚合数据。
- 修正纹理效果：从暧昧薄膜改成覆盖世界表面的网格点状纹理，数据进入后节点发生波峰、波谷、发光、抖动、膨胀等变化。
- 地图本身加入一点淡淡边界和地名，至少能知道空间位置。

本次调整：

- `test0.2.html` 新增 World Map，使用预设聚合数据。该旧版内嵌数据方案已被 `world_data.js` 外部数据文件取代。
- My Radius 保留为个人本地记录功能，只读取 `localStorage`。
- Sense 采样完成后写入 My Radius，不写入 World Map。
- 旧版预设中心和内嵌生成器已删除。
- Canvas 渲染从模糊椭圆膜改为固定网格节点场。
- 每个网格节点根据附近记录计算 `strength / flux / noise / direction`，并表现为亮度、白光、抖动、膨胀和波动。
- 旧版地名已替换为国分寺周边数据文件中的地点。
- `MAP_SPEC` 已同步更新为网格纹理地图层技术方案。

## 2026-05-11

### 按反馈重构 test0.2 视觉与采样流程

用户反馈：

- 地图界面不允许出现任何文字和点状效果。
- 地图层应像一层根据声音和选择控制的、边界模糊的淡膜；单次使用可以很弱，多次使用后显现区域倾向。
- 10 秒采样阶段需要引导用户感受周围环境，参考 Apple Watch Mindfulness/Breathe 的气泡呼吸与触觉提示。
- 采样完成后的选择界面应尽量视觉化，浮现 1 到 3 组算法推断的词语；不合适则 skip。
- 整体切换和文字浮现需要淡入淡出。

本次调整：

- 隐藏 Skin 地图界面的 topbar、说明文字、toast 和工具按钮。
- 移除地图上的记录文字、当前位置点、颗粒点和点状 ambient 效果。
- 将记录渲染改为大面积低透明度、模糊边界的椭圆膜层。
- 边界层改为宽模糊带，不再使用虚线或点线。
- 10 秒采样使用 5 秒吸气 + 5 秒呼气的气泡缩放节奏，并尝试调用 Vibration API。
- 采样后改为浮现词语组：算法按 noise、flux、peak、movement、time slot 排序推荐词；选词即写入，skip 可进入下一组，全部 skip 则无词写入。
- `MAP_SPEC` 新增“薄膜地图层技术实现”“采样呼吸引导”“采样后词语选择”。

技术决策：

- 当前实现直接在主 Canvas 使用大面积渐变椭圆和 `ctx.filter = blur(...)`。
- 后续更稳定的方案应升级为离屏低分辨率标量场：记录写入多个 field channel 后统一模糊、采样、上色、合成。

## 2026-05-11

### 新建 test0.2 原型

用户要求无视 `test0.1.html`，制作 `test0.2` 原型。

本次新增：

- 创建 `test0.2.html`，作为独立单文件移动端 Web 原型。
- 不使用 `test0.1.html` 的 Leaflet 地图结构，改为 Canvas 生成式“我的半径”皮肤层。
- 默认测试中心改为国分寺附近，半径约 1km。
- 使用 `localStorage` 持久化记录。
- 使用 guide book 中的日语感知词：浮く、重い、乾く、ざらつく、眠い、遠い、詰まる、ほどける。
- 实现 Ambient、Contact、Imprint、Boundary、Void 五类视觉状态层。
- 采样支持麦克风输入；麦克风不可用时使用模拟噪声。
- 记录移动状态：still、slow、passing，并转译为沉积或方向性痕迹。
- Radius 视图显示记录数量、空洞单元、平均皮肤强度、时间剖面和感知词分布。

当前限制：

- 仍是单文件原型，尚未拆分模块。
- Canvas 半径是抽象地图皮肤，不显示真实道路和店名。
- 孤岛识别尚未单独可视化。
- 手机端真实麦克风和定位需要通过 `localhost` 或 HTTPS 打开。

## 2026-05-11

### 根据 Endel 参考重构视觉系统

用户要求参考 App：Endel 重构 `MAP_SPEC` 中的视觉系统部分。

本次调整：

- 将视觉系统从“噪点 / 波纹 / 裂缝 / 空洞”的静态分类，重构为更接近 Endel 的状态生成逻辑。
- 新增 Ambient、Contact、Imprint、Boundary、Void 五类视觉状态层。
- 明确视觉变量与数据输入的映射：声音平均值、波动性、噪声峰值、停留时间、移动方向、记录次数、时间段。
- 保留 World Skin 的地图皮肤方向，避免滑向睡眠、冥想或纯抽象声景工具。
- 明确色彩、动效、UI 与地图关系的限制。

参考理解：

- Endel 的关键启发不是具体图形，而是“声音/环境/身体输入驱动生成式视觉”的体验结构。
- World Skin 需要将这种结构转译为城市表面的纹理、痕迹、边界和空洞。

## 2026-05-11

### 根据 guide book 同步项目文件

发现 `guide book` 已有正式内容：

- 标题：《世界皮肤 / World Skin》项目说明书 v0.2
- 文件大小：约 17 KB
- 项目形式：手机端 Web 原型 / 感知型地图系统 / 城市体验可视化实验

本次更新：

- 重写 `MAP_SPEC`，将项目依据从 `test0.1.html` 原型假设切换为 `guide book`。
- 重写 `TODO`，按 guide book 的核心功能与设计原则拆分任务。
- 保留对当前 `test0.1.html` 原型状态的记录，用于对照实现差距。

### guide book 核心要点

项目不是普通地图、打卡工具或点评平台，而是一套以身体感知为基础的地图记录系统。

核心方向：

- 从评价地图转向感知地图。
- 从打卡行为转向身体停留。
- 从地点集合转向强度场。
- 将城市理解为由粗糙度、密度、波动、空白、边界和痕迹构成的皮肤。

核心机制：

- 体感记录。
- 我的半径。
- 空洞与孤岛。
- 边界带。
- 时间剖面。

交互原则：

- 慢。
- 少。
- 现场性。
- 痕迹感。

禁止项：

- 不做评分。
- 不做排行榜。
- 不做成就系统。
- 不做可爱化分享模板。

### 当前代码原型对照

`test0.1.html` 已实现：

- Leaflet 暗色地图。
- 浏览器定位。
- 麦克风采样或模拟噪波。
- 长按 10 秒记录。
- Canvas 声纹动画。
- 底噪与波动性计算。
- 地图噪点。
- 简单个人档案统计。

当前差距：

- 数据未持久化。
- 尚未实现移动状态。
- 尚未实现我的半径。
- 尚未实现空洞、孤岛、边界带。
- 时间剖面仍较粗略。
- 标签词体系与 guide book 不一致。
- 默认位置仍是大阪府河南町附近，不是 guide book 建议的国分寺周边 1km。

### 文件状态

- `guide book`：已存在，且有正式说明内容。
- `setsumei`：IDE 中显示为打开状态，但当前项目目录中仍未找到该文件。
- `MAP_SPEC`：已同步为 guide book 驱动的项目规格。
- `TODO`：已同步为 guide book 驱动的进度清单。
- `MAP_LOG`：记录本次同步。

## 2026-05-11 早前记录

### 初始化项目指导文件

创建文件：

- `MAP_SPEC`
- `TODO`
- `MAP_LOG`

当时 `guide book` 为空，因此第一版文件暂时根据 `test0.1.html` 原型生成。该假设已在本次更新中废弃。

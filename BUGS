# BUGS

更新时间：2026-05-12

用途：记录已发现 bug、修复方式和后续回归检查项，避免同类问题反复出现。

## 已修复

### 001 - 非 Sense 页面也会出现音波【Fixed】

现象：

- 用户在 World Map 或 My Radius 页面按下界面时，也会触发 Sense 采样音波。

原因：

- Sense 采样逻辑没有在 `startRecording` 和 `drawContactLayer` 中检查当前页面。
- 切换页面时没有主动取消正在进行的采样。
- 页面切换只改 opacity，隐藏页面仍可能保留交互风险。

修复：

- `startRecording` 只允许在 `state.view === "sense"` 时开始。
- `drawContactLayer` 只允许在 `state.view === "sense"` 且 `state.recording === true` 时绘制。
- 离开 Sense 页面时调用 `cancelRecording()`。
- 非 active view 使用 `visibility: hidden`，防止隐藏页面控件误触。
- 保持 view 容器本身 `pointer-events: none`，避免 World / Radius 覆盖层挡住真实地图操作。

回归检查：

- 在 World 页面点击、拖拽、缩放，不应出现音波。
- 在 Radius 页面点击、拖拽、打开 details，不应出现音波。
- 在 Sense 页面长按才应出现音波。
- Sense 录制中切换到 World 或 Radius，音波应立即停止。

## 待观察

### 009 - 点阵性能回退【Open / 持续监控】

风险：

- 重新加入逐点 jitter、逐点 pulse、逐点 glow 或无条件逐帧重算后，页面会再次卡顿。

当前处理：

- 默认状态不持续重绘。
- 数据场仅在地图、时间轴、数据源、记录、resize 变化时重算。
- 点阵使用地理米制网格 + spatial hash，不再每帧遍历所有 records × all grid nodes。
- 时间轴变化使用 field 插值。

回归检查：

- 静止不操作时 CPU/GPU 不应持续高占用。
- 地图拖拽、缩放、旋转应能基本流畅。
- `rg "getDotJitter|pseudoNoise|getPulse"` 不应出现运行路径。
- 不应对每个点创建 radialGradient 或 shadowBlur。

### 010 - 点阵与 3D 地图视角脱离【Open / 注意：当前 pitch 已改为 42，非 45】

风险：

- 如果点阵退回 screen-space 网格，会像漂在屏幕前，不再贴合 45 度 MapLibre 斜视地图。

当前处理：

- 点阵先在地理米制坐标中生成，再使用 MapLibre `project([lng, lat])` 投影到屏幕。
- 地图默认 `pitch: 45`、`bearing: -16`。

回归检查：

- 旋转或 pitch 地图时，点阵应随地图表面透视变化。
- 点阵不应像固定在屏幕上的洞洞板。

### 007 - 点阵激活变化失控【Needs Verification / 注意：rust 已从渲染管线移除，当前约束为 alpha/radius/height；感知轴 v2 接入后需重新验证】

风险：

- 为了提高可读性加入 radius、height 和色温变化后，点阵可能退化成柱状图、地形图或霓虹热力图。

当前处理：

- radius 限制在 `0.8-2.4px`。
- height 限制在 `0-96`。
- rust 混合限制在 `0.28` 以内。
- void 区域不抖动、不发光，保持暗点和低高度。

回归检查：

- 点阵仍应保持规则网格结构。
- active 区域可以略亮、略大、略微隆起，但不能变成柱状图、山峰或热力云。
- 少量锈色只能出现在高波动局部，不能铺满整片区域。

### 008 - 地图骨架再次过黑【Open / 持续监控】

风险：

- 如果底图 filter、shade 或 Canvas 底噪过强，道路、水系、铁路和街区边缘会再次消失。

当前处理：

- `MAP_VISUAL_MODES` 默认使用 balanced。
- `M` 键可临时切换 dark / balanced / bright 检查底图结构。
- `.shade` 遮罩已减弱。

回归检查：

- 不放大也应隐约看到道路、水系、铁路和街区边缘。
- 地图仍不得显示地名、POI、marker 或导航路线。
- 点阵数据仍应是主视觉，底图只是城市骨架。

### 006 - 点阵场退化成均匀灰网格【Open / 持续监控】

风险：

- 如果空洞点和 active 点共享过高的 base alpha，整张地图会变成均匀灰色网格。
- 如果 sigma 过大或没有 influence cutoff，所有记录会互相抹平，车站、走廊、住宅和空洞失去层级。

当前处理：

- `strength < 0.025` 的节点只绘制暗空洞点。
- active alpha 使用非线性 strength / contrast / boundary 计算。
- 高斯影响低于 `0.012` 时忽略。
- sigma 随 zoom 控制在 `62-128px`。
- `D` 键 Debug View 可检查 strength / contrast / alpha 的 min、max、avg。

回归检查：

- Debug View 中 `alpha max` 应明显高于 `0.2`。
- `strength min` 和 `strength max` 不应过于接近。
- 车站和通勤走廊应比公园、住宅和空洞更亮、更厚。
- 空洞区域应接近黑色，只保留极暗点阵。

### 005 - World Skin 点阵完全不可见【Obsolete / 根因已修复，保留作历史参考】

现象：

- World Map 打开后看不到任何地图纹理或点阵皮肤。

可能原因：

- `#skinCanvas` 层级低于 `.shade`，点阵被遮罩压暗。
- `#app` 只使用 `100dvh`，部分浏览器或本地打开场景下可能无法得到稳定高度。
- Canvas resize 如果读到 0 尺寸，会导致后续绘制不可见。

当前处理：

- `#app` 增加 `height: 100vh` 作为 `100dvh` 的 fallback。
- `#skinCanvas` 提升到遮罩之上，`.shade` 降到地图与点阵之间的低层遮罩。
- `.view` 和 UI 继续保持在点阵之上。
- `resizeCanvas()` 在 DOM rect 为 0 时回退到 `window.innerWidth / window.innerHeight`。

回归检查：

- World Map 初始页面应能看到规则点阵皮肤。
- 地图暗角和遮罩不能盖住点阵。
- Sense 页面 UI 应仍然在点阵上方。
- 本地直接打开和本地 server 打开时，Canvas 都应有非 0 尺寸。

### 004 - 预设数据退回随机撒点【Needs Verification / 数据已升级为 v4-clean，回归检查仍有效】

风险：

- World Map 的公共聚合数据如果退回圆形范围内均匀随机生成，会破坏真实地图结构感。
- 视觉上会变成随机星空或普通热力图，无法测试通勤走廊、空洞、生活皮肤和孤岛记录。

当前处理：

- `world_data.js` 使用 `WORLD_SKIN_ZONES`、`WORLD_SKIN_CORRIDORS` 和 `WORLD_SKIN_VOIDS` 生成数据。
- 车站、道路/铁路、学校、公园、住宅区和孤岛记录分开采样。
- 空洞区域通过排除区保留，不允许被普通散点填满。

回归检查：

- 国分寺駅与西国分寺駅附近应有更厚的点阵沉积。
- 中央線、西武国分寺線、府中街道方向应出现带状痕迹。
- 公园和绿地应更淡、更松，但不是空白。
- 住宅区应有稀疏连续底层记录。
- 地图上应保留若干低亮空洞。

### 003 - 点阵缩放不同步【Obsolete / 地理米制点阵已是唯一实现，screen-space 回退路径已不存在，保留作历史参考】

现象：

- 地图缩放时，如果点阵使用屏幕固定网格，会出现地图在动但点阵不随真实空间缩放的问题。

当前处理：

- 当前目标已改为地理米制点阵膜。
- 点阵在局部米制坐标中生成，再通过 MapLibre `project([lng, lat])` 投影到屏幕。
- 点阵会跟随地图缩放、旋转、pitch 产生透视变化。
- area / mid / street 层级切换米制 spacing、sigma 和底图强度。

回归检查：

- 放大/缩小地图时，规则点阵应像贴在地图表面上。
- 放大/缩小地图时，记录影响形成的亮区应随地图空间移动和缩放。
- 旋转或 pitch 地图时，记录影响区域和点阵都应跟随地图透视变化。

### 002 - 地图底图可见度【Obsolete / 椭圆膜已删除，当前状态稳定，保留作历史参考】

现象：

- 地图底图可能被 Canvas 暗层、阴影或点阵遮挡。

当前处理：

- 地图页关闭椭圆光膜和同心圈。
- 旧的椭圆膜绘制代码已移除，避免后续误回归。
- Canvas 背景暗层降低到轻微遮罩。
- 底图改为航拍图，并提高显示透明度。

回归检查：

- World 页面应能明确看到航拍地图。
- 中心不应出现椭圆形膜或同心椭圆。
- 点阵应浮在地图上，但不能把地图完全盖住。
